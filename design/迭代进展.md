# 迭代进展与上下文交接

> 本文件是**跨轮次的持久记忆**：每一轮自主迭代开始时先读这里，不要重新全仓扫描。
> 每轮结束前把「已完成 / 已验证 / 下一步」更新到本文件。
> **本文件是项目的完整记录，要详实**：结论、数字、证据链、踩过的坑都要写清楚，
> 不要为了「简洁」而删减 —— 需要压缩的是**对话上下文**，不是这里。

## 2026-09-18 云同步两处逻辑错误修复（用户报障：金小雅 5 单 → 被写成 6）

**用户报障（原话）**：「金小雅原本就有 5 单，然后她又下了 6 单，按照逻辑订单总数本应是 11，
但程序却直接修改成 6」；「另一个协作者已经在表格里写了，她明天的订单是 0（当天不送），
但程序直接修改成 1」。

**根因（改动前）**

| # | 问题 | 位置 |
|---|---|---|
| 1 | 总餐次写**绝对值**：`want = order.meals` 直接覆盖云端 | `app/wps/planner.py` 旧 240-248 行 |
| 2 | 日期格只把「等于 1」当已写，`0` 被判成"还没写"，执行器无条件写 `1` | `planner.py` 旧 239 行 + `executor.py` 旧 303-304、368-370 行 |
| 3 | `build_plan(..., ledger=)` 的 `ledger` 是**死参数**（从未传进 `_build_sheet_plan`），`SyncLedger.synced_meals()` 无人调用 | `planner.py` 旧 392-442 行 |

**下游后果**：`app/ordering/roster.py` 把「日期格 == 1」的人当成当天要下单的名单
（`read_cloud_meal`），所以 bug 2 会真的多送一餐；bug 1 会让顾客已付款的剩余餐凭空消失。

**用户确认的 4 项决策**：① 总餐次 = 云端现值 + 本次增量，账本做幂等锚点；
② 已被写错的云端那一格用户自己在 WPS 里改成 11（不加修复脚本）；
③ 协作者写了 0 时「日期格不动、总餐次照常累加」；④ 本地表批次日期与目标日期不符 → **拒绝上传该表**。

**改动**

* `app/wps/models.py`：`Change` 增加 `local_meals`/`ledger_prev`/`target_occupied`/`local_rows`
  与 `target_blocked` 属性；`CloudOrder` 增加 `order_no`/`rows`/`weekday_marks`；
  `SheetPlan` 增加 `blocked_reason`/`previous_batch`（字段一律追加在末尾，保持位置参数兼容）。
* `app/wps/reader.py`：读取「订单」列与「周一~周日」七列标记（批次日期核对用）；
  **不做**按人合并（多行如何合计待用户定夺）。
* `app/wps/planner.py`：
  - 新增 `_batch_date_check()`：本地表星期标记与目标日期不符 → `blocked_reason`，
    **在发出任何云端请求之前**返回（被拒绝的子表零额度消耗）；
  - 新增 `_sum_rows_per_person()`：同一个人多行时**每行算 1 餐相加**（用户 2026-09-18 明确）
    并给出提示——旧实现会给同一人写多条变更、互相覆盖（只算最后一行）；
    累加语义下那还会让回读校验失败 → 账本不记账 → 重复加餐；
  - 老客户：`总餐次 = 云端现值 + (本地合计 − 账本本批已同步)`；增量 0 → 一个字都不写；
    增量 < 0（退单）→ 不减、只警告；目标格被协作者占用 → `target_occupied`，只读不写；
  - 本地「餐次」为空/0 → 跳过该人 + 警告（否则会白送一餐）；
  - 云端已有同名不同电话的人 → 新增前警告；账本有记录但云端找不到人 → 警告；
  - `summarize_plan` 增加 `skipped`；`format_plan` 写清「本次 +N / 本批已同步多少 / 协作者写的那格」。
* `app/wps/ledger.py`：新增**只读**访问器 `synced_local()`（不用 `setdefault`，并发安全）、
  `synced_total()`；`record()` 支持 `{"local", "total"}`（兼容 int）；
  旧账本的 `meals` 字段回退当"已同步的本地餐次"读 → **升级后同一批不会重复加**。
* `app/wps/executor.py`：`blocked_reason` → `stale_batch` 状态（不写任何格子）；
  占用格不写 `1`；回读校验的期望值 = 协作者原值；账本记 `{local, total}`；
  账本保存失败升级为 ERROR + "下次可能重复加餐"提示。
* `app/api/bridge.py`：`stale_batch` 逐表日志、汇总行加「跳过 N 人」、
  被拒绝的表在 `failed_sheets` 里如实报告。
* 前端：`WpsPlanSummary.skipped`、CloudForm 汇总行与两处说明文字。
* 工具：`tools/simulate_wps_fixture.py` 从失效状态（`from app import wps_cloud`，
  该模块在重构后已不存在）修好并升级验收项（累加、0 保留、幂等）。

**证据链**

* 本地表实测：`~/下载/yikou-light-food-linux-x64/排单.xlsx` 里金小雅（W17/D2/13175798579）餐次 = 6；
  批次星期标记全是「周六」= 目标日期 2026-09-19 的周几；云端（截图）总餐次 6 / 已出餐 5 / 剩余 1。
* 账本实测：`~/.config/yikou-light-food/wps_sync_state.json` 里每批每人的 `meals` 值 = 当时的本地餐次
  （所以旧文件可直接当幂等锚点）；9.17 那批里「雷丝淇」两单各 1 餐只记了 1、
  「柟」W33 两行（x2 份）只记了 1 —— 这是"只取最后一行"的线上证据。
* 离线仿真（`tools/simulate_wps_fixture.py`，夹具 `~/文档/测试东湖中餐.xlsx`）：
  102 人 + 14 个新客户，**总餐次 = 云端原值 + 本地餐次**、**协作者写的 34 个 `0` 原样保留**、
  第二次上传零改动（旧程序会把那 34 格全改成 1）。

**验证**：`pytest tests/` 950 → **961 passed**；`ruff check app tests scripts tools` 通过；
前端 `pnpm build`（tsc + vite）、`pnpm lint`、`pnpm test`（67 passed）通过；
`tools/simulate_wps_fixture.py` 通过。

**一人多行 = 云端多行（用户 2026-09-18 明确）**：本地同一个人多行时（同一晚两单 /
一单 x2 份写成两行）**云端也写同样多的行**，每行各标当天 1 —— 「闪时送下单」
（`app/ordering/roster.py` 按"日期格 == 1"逐行出单、`payload.py` 每单 goodsNum=1）
就会下两单、这天真的送两餐。实现：`planner._group_rows_per_person()`（分组，本地第 i 行
↔ 云端第 i 行 = 槽位）、`executor.read_person_rows()`（回读行号**列表**）、
账本 `{"local", "slots", "total"}`（按槽位幂等）。
**这是本轮最"重"的一处改动**：匹配/排序/回读/记账全部从"一人一行"改成"一人多行（槽位）"。
副作用（用户已知悉并接受）：协作者的表里同一个人会出现多行；程序按表内顺序把它们当作
第 1、2…行，重复上传不会多加行；若协作者手工调换了这两行的先后，行内金额可能对调
（总数不变）。

**发布待办**：版本号 `android/version.properties` 3.6.9 → 3.6.10（versionCode 3061000）
与 `app/__init__.py` 同步；手机端用应用内更新升级；**升级后第一次上传先点「预览」**核对
（除金小雅外不应出现任何 `+N`），并由用户手工把云端金小雅的总餐次改成 11。

## 发布记录：v3.5.0（2026-09-16）

自 v3.4.1 起共 32 个提交。**用户明确要求发布**（此前的约定是「只推 main，不自动打标签」，
本次由用户指示打标签发布）。

**面向用户的变更**

* **执行效率**（第 2、3 轮）：云文档同步的两条链路改为分批并发，各约 **2.00x** 提速
  （`wps_check_copies` 1.803s → 0.902s；`build_plan` 2.711s → 1.357s），
  **云端调用次数分毫不变**，不额外消耗每日额度。
* **行为变更**（第 21 轮，用户已确认）：
  1. `save_wps_config` 收到**部分** `tables` / `address_order` 时，没提到的子表**保持原样**
     （原先把它们重置成出厂值）；
  2. 6 个字段自动去首尾空格（`target_url`、`phone_number`、`sss_account`、
     `sss_product_name`、`sss_common_address`、`sss_fixed_address_detail`），
     其中电话与闪时送账号同时是**密钥链账号名**，去空格可避免「密码存过却取不到」。

**内部质量**：Python 测试 415 → **861**；前端测试 2 → **28**；
`app/` 下公开接口 docstring 缺口 105 → **0**；前端纯函数抽出为 `lib/format.ts` 便于测试。
**版本号 3.4.1 → 3.5.0**（存在用户可见的行为变更，故走 minor）。

## 硬约束（每轮必须遵守）

1. **功能不变原则**：不改动核心业务逻辑、对外接口与既定功能行为，只做**非侵入式**
   优化（可维护性 / 执行效率 / 工程化规范）。
   *自检*：`git diff 01308b6..HEAD -- app/ | grep '^-' | grep -v '^---'` 应为空；
   `sss.py` / `wps_cloud.py` / `bridge.py` / `automation.py` / `updater.py` /
   `frontend/` 应**零改动**。
2. **每轮推送 GitHub**：`git push origin main` 完成版本备份与归档。
   推 `main` 只触发 Tests；**打 `vX.Y.Z` 标签才会触发公开 Release 构建**——
   已与用户确认：**不自动打标签**，发版时机由用户决定。
3. **上下文压缩由 DSH 原生提供**（见下），不要自己在项目里造日志压缩机制。
4. **改前先实地考察，改后必须反证**（用户 2026-09-16 明确要求）：
   1. **先考察、再自问必要性**。动手前尽量**登录真实网站/云端实际操作一遍**
      （管理后台 `m.icall.me`、闪时送、WPS 云文档），看清现在的真实行为，然后回答
      一个问题 —— **「这个改动到底有没有必要？」** 没必要的改动一律不做：只为
      「看起来更优雅」而动代码，收益为零、风险为正。改动前先读 `design/` 里已归档的
      真机记录（`WPS真机验证清单-*.md`、`WPS基线存档.json`、`SSS-TIMING-FINDINGS.md`、
      `sss-timing-raw/`），不要凭空推测线上行为。
      *诚实边界*：我（AI）**没有用户的账号，无法登录真实站点**。因此凡是需要真机
      才能确认的改动，必须**显式说明「未做真机验证」并请用户在真机上复核**，
      绝不能把「单测通过」说成「功能已验证」。
   2. **改完重新验证可行性**。要在**真实链路**上确认它确实可用，而不是只看单测变绿。
      单测全绿 ≠ 功能可用；新增的测试必须能证明**真实场景**成立（例如断言真实返回
      结构、真实调用参数），而不是只把实现复述一遍。
   3. **先想衍生 bug**。问自己这个改动会不会波及其它路径：并发会不会触发对端限流
      （`429002` 不被重试）、失败路径会不会多发请求、异常类型与时序有没有变、
      Windows 与 Linux 表现是否一致（换行/文件锁/**路径分隔符**）、旧配置与旧账本
      还能不能读。**改一处、想三处。**
      *真实案例（第 13 轮踩到，第二次）*：**CI 上不要写任何依赖墙钟的断言**。
      第 2 轮写的 `test_concurrency_is_faster_than_serial` 用「并发实测 < 串行实测
      × 0.75」，macOS runner 上因线程启动开销只到 1.17x 而**假失败**；改成与
      「`N × delay` 理论下界」比较后**仍然假失败** —— 因为 macOS runner 上
      `time.sleep(0.03)` 实际要花 60~130ms（10 次读 / 4 并发跑了 0.579s，而下界只有
      0.300s）。**连 sleep 时长本身都不保证**，所以下界也一样不可靠。
      正确做法：并发的证据改用**确定性**指标（同时在途的请求数 `max_inflight`），
      彻底不碰计时。两处墙钟断言已删除。

      *真实案例（第 6 轮踩到）*：新写的测试里写死了 POSIX 路径字面量
      `Path("/tmp/排单 名单.xlsx")` 并断言 `== "/tmp/排单 名单.xlsx"`，Linux/macOS
      全绿，**Windows 作业直接失败** —— `str(Path("/tmp/x"))` 在 Windows 上是
      `"\\tmp\\x"`。教训：**断言路径一律用 `str(tmp_path / ...)` 现算**，
      不要写死分隔符与盘符。这条约束是本轮自己刚写下的，本轮就自己违反了一次。
   4. **做好反证**。主动**推翻自己**，而不是收集支持自己的证据。现成手段：基线整树
      检出 + 随机场景差分对拍、变异测试（注入真实 bug 看测试是否 FAIL）、AST 比对
      对外接口面、`git diff -w` 清点删除行、调度反证子代理独立复核。
      **找不到反例不等于没问题，只说明还没试够。**

## 基线（每轮验证命令）

```bash
.venv/bin/python -m pytest tests -q          # 期望：861 passed, 4 skipped
.venv/bin/python -m ruff check app tests scripts
.venv/bin/python scripts/check_version_consistency.py
.venv/bin/python -m app.main --self-check
python scripts/check_workspace_hygiene.py --working-tree   # 见「已知本地噪声」
```

**每轮必做的功能回归校验**（这是「功能不变」的证据链，缺一不可）：

```bash
# 1) 对外接口面零变化（AST 比对 Bridge 的公开方法名/签名）
# 2) app/ 与 frontend/ 的业务语义零变化（看 diff，确认删除行都是结构搬移）
git diff 01308b6 --stat -- app/ frontend/
# 3) 全套测试 + lint + 编译 + 自检 + 版本一致性 + 前端 lint/test
```

## 项目速览

- 桌面程序：pywebview（React + TS + Tailwind 前端）+ Playwright + openpyxl。
- 三个任务模式：订单处理、云文档同步（WPS 云端排单表增量写入）、闪时送下单。
- 默认**纯接口模式**（不发浏览器），可在界面切回浏览器模式。
- 关键模块：`app/bridge.py`（js_api 桥 + 事件通道）、`app/sss.py`（闪时送）、
  `app/wps_cloud.py`（WPS 云表）、`app/updater.py`（Ed25519 签名自动更新）。
- 版本号唯一来源：`app/__init__.py`；发布靠推 `vX.Y.Z` 标签触发三个平台工作流。
- 基线提交 `01308b6`（v3.4.1）是「功能不变」的比对基准。

## 关于「上下文自适应压缩」（第 1 轮的结论，重要）

用户原话澄清：**「100M 指当前这个对话内容的上下文，快要逼近 100 万 token 的时候
自动压缩当前对话的上下文，而不是项目里面的日志。」**

**第 1 轮曾误读为「程序 logs/ 目录的 100 MiB 预算」**，并实现了
`app/artifact_store.py`（启动时后台压缩/删除旧失败快照）。该实现已**全部撤销**
（`git checkout 01308b6 -- app/`，`app/` 现与 v3.4.1 逐字节相同）——它既不是用户
要的东西，也违反上面的「功能不变原则」。如需找回：`git show 3a24ef1`。

**正确结论：DSH 原生已提供对话上下文自动压缩，无需（也无法）在项目里实现。**

- 实现包：`@deepseek-ai/dsh-compaction-basic`，由 `dsh-base` 默认挂载，
  同层还有 `dsh-command-compact`（`/compact`）与
  `dsh-compaction-tool-result-pruner`（`thresholdChars: 8192`）。
- 默认策略：`thresholdRatio: 0.8`、`retainRatio: 0.16`、`auto: true`、
  `compactionRetries: 1`、`maxOverflowRetries: 1`。
- 当前模型 `deepseek-flash` 的 `contextWindow: 1000000`（见
  `~/.dsh/settings.yaml`），因此**自动压缩在约 80 万 token 触发**，
  并原样保留最近约 16 万 token；这正是用户要的「逼近 100 万时自动压缩」。
- 触发时机还会在 provider 明确返回 `CONTEXT_WINDOW_EXCEEDED` 时兜底：
  压缩一次并重试。
- 调参入口：`~/.dsh/profiles/web/cordis.patch.yml`（当前为空 `[]`）。
  ⚠️ 该文件校验严格（未知字段/比例不合法会**拒绝加载插件**），
  改动前务必确认 schema，且改完全局生效、需重载 DSH。

## 已完成

### ISSUE-05（2026-09-20/21）：手机提示条遮挡任务状态区 —— 改为「占正常布局空间的提示区域」

**报障**：上一轮用固定像素偏移（`--safe-top + 84px`）把提示条挪开日志按钮，但那只把遮挡
挪到了别处：提示条仍浮在任务工作台头部的**权威状态区**上方，长提示/多条提示/字体放大/
横屏时偏移量还会失配（偏移是写死的，内容高度不是）。

**结论：不再调偏移量，改成占正常布局空间的提示区域（要求里的首选方案）**

- `App.tsx` — 手机端在任务面板上方渲染 `[data-phone-notice-region]`（`.phone-notice-region`），
  sonner 的 `<Toaster>` 渲染在这个区域里；区域用 `ResizeObserver` 实测高度与底边，
  注入根节点 CSS 变量 `--phone-notice-height` / `--phone-notice-bottom`（空闲时都是 0，
  布局与改动前逐像素一致）。桌面/平板不渲染该区域，`<Toaster position="bottom-right">`
  一行未动 → 桌面行为不变。**删除了上一轮的 `offset`/`mobileOffset` 固定偏移。**
- `index.css` — 只覆盖 sonner 的**定位/堆叠**：容器 `position: static`、每条提示
  `position: relative` + `transform: none` + `height: auto`（去掉浮层用的
  `--initial-height` 写死高度，否则字体放大/多行长提示会被截断）、`touch-action: auto`
  （允许区域内滚动看长提示）。不动它的展示时长、配色与关闭按钮。
- `LogConsole.tsx` — 全屏日志层的 `fixed inset-0` 会盖住页面流，所以它的内容顶部改为
  `max(var(--safe-top), var(--phone-notice-bottom, 0px))`：有提示时整体让到提示区域之下，
  空闲时退回原来的 `safe-top`（水波圆心/半径完全没动，第 7 节的圆心断言四种几何仍全绿）。

**为什么这样就同时满足全部约束**

| 关键内容 | 位置关系 |
| --- | --- |
| 右上角日志按钮 | 标题栏带（fixed，顶部）；提示区域在标题栏**下方**的文档流里 |
| 权威任务状态区 | 任务工作台头部，在提示区域**之下**（被推下去，不可能被盖） |
| 失败原因横幅 | 与提示区域同属文档流，且在它**上方**（`AuthBanner` / 全局请求错误 / 待恢复横幅） |
| 底栏「开始/停止」 | flex 列最底，区域在顶部 |
| 待处理输入（日志内地址补全） | 日志层内容，已按区域底边整体下移 |
| 确认弹窗 | Radix 覆盖层 `z-50` > 提示区域 `z-40` |
| 长提示/多条提示 | 区域 `max-h-[min(45vh,calc(100vh-15rem))] overflow-y-auto`：常规视口按 45vh，极短视口按「视口高 − 15rem」（15rem ≈ 标题栏 + 任务面板固定 chrome 的实测上界，随字体缩放一起长），超出则区域内滚动 |
| 字体放大/横屏/键盘 | 高度实测 + `vh` 上限 + 流式排布，没有写死的像素假设 |

**验证（本机真跑）**

- `npm test` 205 passed；`npx tsc -b` 0 错误；`npm run lint` 0 warnings 0 errors；`npm run build` 成功。
- `node scripts/browser-interaction-check.mjs`：**250 PASS / 0 FAIL**（含新增第 9 节 13 条）：
  1. 无提示时区域高度为 0、标题栏仍从安全区起、主内容紧接标题栏（不改变原布局）；
  2. 有提示时区域占位（高度 > 0）、主内容从区域下方开始；
  3. 提示不遮右上角日志按钮（区域/提示条与按钮矩形都不相交 + 按钮中心命中按钮）；
  4. 提示不遮**任务工作台头部的权威状态区**（不相交 + 状态胶囊可命中 + 状态文案/说明仍在）；
  5. 提示不遮**失败原因横幅**（真实渲染「属于其他账号」脱敏横幅：不相交 + 文案仍在 + 可命中）；
  6. 提示不遮**待处理输入**（真实渲染待确认地址输入：在区域之下 + 可命中）；
  7. 提示不遮**确认弹窗**（真实点击「开始处理」弹出确认框：矩形不相交 + 弹窗内按钮中心全部命中自身）；
  8. 多条提示在区域内依次排布（两两不重叠）且区域高度随之增长；
  9. **字体放大 20px**：区域高度增长但仍 ≤45vh 上限，日志按钮与底栏主按钮仍可点；
  10. **横屏 568x320**：区域 ≤上限、不遮日志按钮、底栏主按钮仍可点；
  10b. **最极端组合（568x320 + 根字号 20px + 多条提示）**：区域被上限截住并可滚动，
       底栏主按钮仍在屏内可点 —— 这条最初是 FAIL：区域按 45vh 取到 131px 时，
       任务面板自身的固定 chrome（头部/页签/底栏 ≈ 293px）加上提示区域会把
       「开始/停止」挤出 320px 高的视口（实测按钮 y=424）；加上
       `calc(100vh - 15rem)` 这条安全线后区域退到 20px 并区域内滚动，底栏回到屏内；
  11. **键盘顶起 390x430**：区域仍在可视区内，日志按钮与底栏主按钮仍可点；
  12. 桌面 1280x800：**没有**提示区域元素，提示仍是 sonner 默认的右下角浮层。
  第 8 节原有的「真实鼠标点击开关日志 / 不遮搜索与工具入口」等断言同步改成
  「日志头部整体让到提示区域之下」的判据，全部继续通过。

**未验证**：真机 WebView 的显示效果与时长；APK 未构建（本机无 JDK/Gradle，任务也不含出包）。

### 用户报障（2026-09-20）：手机端提示条压住日志退出按钮（C 报告 P1）

**形态（C 的复现步骤）**：手机布局 → 打开运行日志 → 工具菜单「复制」→ 提示条贴顶弹出，
矩形 `(16,16)-(374,69.5)` 与右上角日志按钮 `(338,0)-(378,40)` 相交 → 约 4 秒内
`elementFromPoint(按钮中心)` 命中 toast，按钮点不到（同一条 toast 也会压住标题栏主题切换）。

**根因**：`App.tsx` 的 `<Toaster position="top-center">` 在手机端用的是 sonner 默认偏移
（`--mobile-offset-top: 16px`，`@media (max-width:600px)` 生效），而日志按钮是 `fixed`
在 `--safe-top` 起、高 40px 的常驻按钮 —— 提示条没有为它让位。层级本身没问题
（toast 容器 `z-index: 999999999`，按钮 `z-40`），问题纯粹是**几何**。

**修复（只动提示条相关代码，业务/日志逻辑零改动）**

- `App.tsx` — 手机布局给 `offset` 与 `mobileOffset` 传 `{ top: PHONE_TOAST_TOP }`，
  把 `top-center` 整体压到 `--safe-top + 84px` 之下；桌面/平板不传 offset，
  仍是 sonner 默认的右下角。**没有**提高按钮层级、**没有**取消提示、**没有**缩短时长。
  - 84 = 日志头部折行后底边 78 + 6px 呼吸间隙；下限取 `FAB.sizePx + FAB.clearanceGapPx`
    （48px，按钮尺寸变化时自动跟着走），两者取大。
  - 为什么是「下移」而不是「右移」：日志头部在窄屏 + 搜索展开时可能折行，
    横向让位仍会压住搜索框/工具入口（门禁实测：360 窄屏 + 搜索展开时头部一行 12→44，
    提示条若贴顶就会盖住这一行）。

**验证（本机真跑）**

- `node scripts/browser-interaction-check.mjs`：**188 PASS / 0 FAIL**，其中新增第 8 节 11 条
  真实渲染 + **CDP 真实鼠标事件**（走浏览器命中测试，不是 `el.click()`、也不只看 z-index）：
  1. 成功提示显示期间：提示条与按钮矩形不相交、按钮中心命中按钮本身；
  2. 成功提示显示期间：真实点击按钮**立即收起**（状态确实变化，提示没被点掉），再点一次立即展开；
  3. 失败提示显示期间（Clipboard API 抛错 + `execCommand` 返回 false 两条路径都失败）：同样能立即收起；
  4. 连续 3 条提示叠起来仍能真实点击退出；
  5. 窄屏 360 + 搜索展开：提示条落在头部之下、不遮搜索框/工具入口且两者仍可点；
  6. 最坏情况（运行中长状态「闪时送下单执行中」+ 搜索展开 + 360 窄屏）同样成立；
  7. 手机端提示条仍在顶部区域（`top < innerHeight/2`，证明没有机械地挪到底部）；
  8. 底栏主动作：提示还在时真实点击「开始处理」仍能打开确认弹窗；
  9. 手机横屏 568x320：不相交、真实点击能收起；
  10. 刘海安全区（`--safe-top=24px --safe-right=44px`）：按钮与提示条一起下移，关系不变；
  11. 模态弹窗（DecisionDialog = Radix AlertDialog）仍拦住背后的按钮：真实点击按钮坐标
      既不开合日志、也不关闭弹窗；
  12. 桌面 1280x800：提示条仍在右下角（手机端偏移不外溢）。
- `SCENARIO=toast-covers-log-fab node scripts/mutation-check.mjs`：把提示条退回贴顶
  → 4 条预期断言 FAIL、4 条控制组 PASS（证明第 8 节断言不是空转）。
- 顺手修掉 7g 的一处时序抖动：探针原本在 480ms 水波动画中途取样（`inert` 一翻就探），
  改成等「面板中心真的被盖住」再探，避免偶发 FAIL。
- `npm test` 205 passed / `npx tsc -b` 0 错误 / `npm run lint` 0 warnings 0 errors /
  `npm run build` 成功。

**未验证**：真机 WebView 的提示条位置与时长、软键盘顶起与旋转下的实际表现；
APK 未构建（本机无 JDK/Gradle，本任务也不含出包）。

### 用户报障（2026-09-20）：手机端日志「退不出来」——入口在底栏、动画与覆盖层仍按右上角按钮设计

**报障形态**：点底栏的日志按钮能展开全屏日志，但全屏层盖住了底栏、面板内部又没有
关闭入口 → 用户被困在日志层里；水波圆心也还在右上角（按钮已经不在那儿了）。

**只读定位（4 处，根因明确）**

| # | 位置 | 问题 |
|---|---|---|
| 1 | `frontend/src/App.tsx`（手机分支） | 渲染了全屏日志层，却**没有挂载**已有的 `LogFab`（组件成了死代码） |
| 2 | `frontend/src/components/TaskPanel.tsx` `BottomDock` | 日志入口被放在底栏（云文档页签的 `CloudForm` 底栏另有一颗） |
| 3 | `frontend/src/lib/reveal.ts` `LOG_REVEAL_ORIGIN` | 水波圆心表达式仍固定在右上角按钮中心 |
| 4 | `frontend/src/components/LogConsole.tsx` `PhoneLogSheet` | 全屏层 `z-30` 盖住底栏，层内没有关闭入口；关闭动画的 `element.animate()` 无异常兜底 |

**修复（前端 6 个文件，后端零改动；业务互斥/提交/账本/恢复逻辑零改动）**

- `App.tsx` — 手机分支恢复 `<LogFab open={reveal.open} running={workerAlive}
  onToggle={() => reveal.toggleFrom(null)} />`（`z-40` > 面板 `z-30`，全屏时仍可点它收起）；
  并在根节点注入 `--fab-clearance`（仅手机布局），供标题栏/日志头部让位；
  启动任务自动展开的逻辑（`workerAlive` 上升沿 → `openRemembered`）原样保留。
- `TaskPanel.tsx` / `CloudForm.tsx` — 删掉底栏的日志按钮与 `logReveal` 透传（连带
  `pointFromEvent` / `LogReveal` / `ScrollText` 引用），底栏只剩「主按钮 + 停止」。
- `lib/reveal.ts` — 新增 `FAB_CLEARANCE`：与 `LOG_REVEAL_ORIGIN` **同源**（都从
  `FAB.rightPx / FAB.sizePx` 推导，+8px 呼吸间隙），改按钮尺寸时圆心与让位一起变。
- `TitleBar.tsx` — 第一行 `pr-[var(--fab-clearance,0px)] sm:pr-4`，主题切换不再被压住。
- `LogConsole.tsx` —
  * 日志头部第一行同样让位（过滤输入框/自动滚动/清理常规都不被按钮压住）；
  * 面板裁剪值改为**始终由 React 显式写入**（展开=满圆、收起=半径 0），不再依赖动画
    结束后残留的内联样式 —— 否则「无动画 API 且此前关过日志」时重开会永久不可见；
  * 关闭三级降级：① 无 WAAPI / 减弱动效 → 直接落终态；② `animate()` 抛错 → 落终态
    （旧代码会让 `phase` 永远停在 `closing`：全屏不透明、点不动 = 被困住）；
    ③ `onfinish/oncancel` 都没来 → `closeMs+150ms` 定时器兜底；
  * 收尾用**代次（generation）+ ref 定时器**判定迟到回调，不再用 effect cleanup：
    effect 依赖里有 `phase`，用自己的 cleanup 作废收尾会在 `setPhase('closing')` 之后
    **立刻误杀本次关闭**（浏览器门禁实测抓到：面板停在 `closing`）。快速开关因此不会
    留下遮罩或错误状态。

**验证（本机真跑，非推断）**

- `pnpm test`（= `node --test src/lib/*.test.ts`）：**205 passed / 0 failed**（本次新增 2 条
  `FAB_CLEARANCE` 与圆心同源、让位宽度不小于按钮占位的用例；其余增量来自并行的日志显示/剪贴板用例）；
- `npx tsc -b` 0 错误；`pnpm lint`（oxlint）0 errors（85 文件 / 116 规则；余 2 条 warning
  在并行会话新写的搜索/日志显示代码里，属其收尾范围）；
- `pnpm build` 成功；产物 CSS 里 `.pr-[var(--fab-clearance,0px)]` 在基础层、
  `.sm\:pr-4` 在 `@media (width>=40rem)` 且更靠后 → 桌面恢复普通内边距（不回退）；
- `node scripts/browser-interaction-check.mjs`（本机 Headless Chromium 149）：
  **171 PASS / 0 FAIL**，其中日志按钮/水波相关 23 条真实渲染 + 真实点击断言（本次新增第 7 节）：
  唯一入口/底栏只剩业务动作、按钮与主题切换及日志头部控件不重叠（矩形 + 命中测试）、
  **水波圆心 = 按钮中心（用探针元素让浏览器布局引擎解析 `clip-path` 的 `calc(100% - …)`，
  实测 dx=dy=0，竖屏/横屏/刘海安全区/软键盘四种几何都成立）**、展开→同一按钮收起、
  运行中自动展开后可退出（停止按钮仍可用、`stop_task` 0 次、筛选草稿保留 = 关闭只隐藏界面）、
  快速点 5 次/再 1 次落一致态且底栏主按钮仍可点、减弱动效、`animate` 缺失/抛错、
  390x844 / 568x320 / 360x800、`--safe-right=44px --safe-top=24px` 刘海场景、
  软键盘（390x430）、桌面 1280x800 无悬浮按钮且日志仍是常驻分栏；
  R8 异常门禁：6 条预期内（都是故意断网的 resourceError）/ 0 条未预期；
- `scripts/mutation-check.mjs` 新增两个变异场景（`log-fab-not-wired`、
  `log-close-stuck-closing`），要求指定断言必须 FAIL、控制组必须 PASS —— 证明新断言不是空转
  （实测：前者 4 条预期 FAIL + 5 条控制组 PASS；后者 2 条预期 FAIL + 4 条控制组 PASS）。

**并行会话（重要）**：本轮期间另有一条会话在**同一工作区**重做日志头部（搜索图标入口 +
工具菜单，新增 `lib/logDisplay.ts`、`lib/clipboard.ts`）。因此：

- `LogConsole.tsx` 的关闭收尾一度被对方的整段重写**覆盖回旧版**（`settled` + effect cleanup，
  即门禁抓到的「停在 closing」写法），已按代次方案重新落回；对方的头部改版本身保留；
- 门禁里日志头部的锚点由对方同步改成 `data-log-search*` / 日志工具菜单（断言语义不变）；
- 复核结论：`tsc -b` 0 错误、`npm test` 205 全绿、浏览器门禁 171 全绿都是在**合并后的当前树**上跑的。

**环境**：本机原本没有 Chromium，为跑门禁 `pkg install x11-repo && pkg install chromium`
（Termux 侧开发依赖，不进仓库、不影响产物）。`frontend/node_modules` 用 `npm install` 装
（仓库用 pnpm，本机没有 pnpm；仅本地验证用，未改 lockfile）。

**遗留**：手机端最终效果需要**新 APK** 才能验收（本机无 JDK/Gradle，无法出包）。

### 用户需求（2026-09-20）：日志重复展示与常驻工具精简 + 手机任务页清单（批 1 / 批 2）

**报障形态**：日志里同一份内容同屏出现两次 —— 已逐字段显示完整的订单摘要仍带「展开明细」，
点开是全文副本；长单行（>120 字但只有一行）也给展开入口；展开是「摘要 + 追加全文」而不是替换。
头部另被搜索框、级别筛选、复制、清理、自动滚动占满。

**批 1（已完成，只动展示层）**

- 新增 `lib/logDisplay.ts`（纯函数、可单测）：**能不能展开只看「是否真的有隐藏内容」**——
  订单摘要永不折叠；普通日志只有存在非空后续行才可展开；长单行直接显示完整内容（长度不再是依据）；
  展开是**原位替换**摘要；搜索命中被折叠的行时强制展开，并把第一条命中滚进可视区；
  跨行／跨字段命中整块标记；复制取**完整原文**（不改写 `｜` 分隔符）。
- `components/LogConsole.tsx`：常驻只留**标题 + 一个任务状态 + 关闭入口 + 正文**；
  搜索改为**图标入口**（关闭搜索同时清筛选，不留「看不见的筛选」）；级别筛选／自动滚动／复制／
  清理收进**工具菜单**，警告／错误条数以角标留在入口；默认级别「全部」（警告与错误不默认过滤）；
  待确认地址输入仍渲染在正文上方、不进菜单；删除日志尾部装饰标语。
- 新增 `lib/clipboard.ts`：Clipboard API 失败时退回 `textarea + execCommand`，两条都不通时
  提示「长按手动选择」—— 非安全上下文／权限被拒的真机 WebView 上不会静默失效。
- 折叠只影响渲染：`fullLines` 始终保留全部行，过滤与复制都基于原始 `msg`；不改下单与云同步行为。

**验证**：`npm test` 205 全绿（新增 `logDisplay.test.ts` 14 条、`clipboard.test.ts` 6 条）；
`npm run lint` 0 warning；`npm run build` 成功；浏览器门禁 **171 PASS / 0 FAIL**，其中新增
7f／7g 用**合成日志**（长单行／多行／订单摘要／错误行）覆盖折叠不重复、展开原位替换、
搜索揭示隐藏命中、复制完整原文、运行中开关日志；`SCENARIO=log-duplicate-expand` 变异
（可展开退回按长度／展开追加全文／搜索不揭示隐藏命中）要求 5 条断言必须 FAIL、
5 条控制组必须 PASS —— 实测通过，证明新断言不是空转。

**批 2（用户确认方案后已实施）**：权威运行状态集中到「任务工作台头部」一处 ——
标题栏删除重复的 `状态：… · …`（品牌／版本／任务类型／安全模式／主题保留）；订单、闪时送、
云文档三个页签内重复的 `operationView` Callout 删除（删除前逐项核对：三处都只有
`label + detail`，无独有错误原因、无恢复按钮、无处置提示；云文档页该 Callout 是该文件里
`operationView` 的唯一用处）；闪时送「本次执行方式」重复说明块删除（三句后果说明的语义已由
模式选项 description、主按钮文字、真实下单确认弹窗与安全模式承载）；`FlowStrip` 保留、只去掉
「结果」步骤重复的 `detail`；订单页「保存到系统凭据管理器」移入高级设置（默认值／保存语义／
清除凭据行为未改）。头部说明行由单行 `truncate` 改为允许折行 —— 否则删掉页签 Callout 等于把
长失败原因一起藏掉（实测：失败态头部说明高 17px、被省略号截断，改后 50–66px 完整可见）。

**批 2 验证**：门禁新增「批2 三页签状态矩阵」（三页签 × 就绪／运行／成功／失败／待核对 =
15 格 + 窄屏 360×800 + 横屏 800×450）与「批2 草稿与高级设置」。同一套断言在改前构建上
**27 条 FAIL**（15 格全部检出重复状态块、失败／待核对态检出说明被截断），改后 **237 PASS / 0 FAIL**；
`operationSurfaceJs` 按「保留的权威状态区」重写并**收紧**（原来要求页签内存在重复块，现在要求
页签内没有重复块且说明不被截断），断言名不变以免削弱变异场景。新增变异场景
`batch2-duplicate-status-restored`／`batch2-header-detail-truncated`／
`batch2-remember-default-flipped` 证明新断言不是空转。

**遗留**：日志与任务页的最终观感需要**新 APK** 真机确认（本机无 JDK／Gradle，无法出包）。

### 用户需求（2026-09-18）：手机端日志改为「悬浮按钮 + 水波扩散」

用户原话：*「我想把日志设计成一个悬浮按钮的形式，点一下会从悬浮按钮那边像水波
一样扩散，点击开始处理等按钮也会自动扩散出日志，现在 github 里面找找有没有符合
我这个要求的项目能直接移植的，然后做出修改计划，有什么不确定需要问我向我确认」*。

**先调研、后自研**：`github.com` / `cdn.jsdelivr.net` 在本机被 DNS 阻断，改从可访问
的 npm registry 下载各库的**发布产物源码**逐个读。结论是**没有能直接移植的**：

| 库 | 状态 | 不能直接用的原因 |
|---|---|---|
| `react-circular-reveal` | 1.2.2，2019 发布 / 2022 最后更新 | webpack UMD、无 ESM/类型；语义是「覆盖式幕布 + 自动追踪鼠标」，不是底部面板；塞进 `vite-plugin-singlefile` 产物会破坏 tree-shaking |
| `react-theme-switch-animation` | 1.3.0，2026-08，支持 React 19 | 基于 View Transitions API：**动画期间整页是快照**，日志停止刷新且不可交互；绑定 Tailwind 深色主题语义 |
| `material-design-transform` | 3.0.0 | 同为 View Transitions 容器变形（快照），额外依赖 `react-ref-composer` |
| `@aphrody/m3-motion` | 3.2.5 | 依赖 `motion`(framer-motion) ≥12，约 +50KB，与单文件零重依赖策略冲突 |
| `floaty` / `circular-revealer` | 2022 前后停更 | 前者是悬浮操作条不含圆形扩散；后者无类型、jQuery 式 API |

它们内部用的是**同一个套路**（圆心到目标矩形四角最大距离为半径 + 圆形遮罩），
所以移植的是套路不是包 —— **自研约 150 行，零新依赖、零 lockfile 变更**。

**用户确认的 6 项设计决策**（提问后逐条拍板）：
1. 生效范围：**仅手机布局**（<640px），平板/桌面维持日志常驻分栏；
2. 扩散起点：**被点击的那颗按钮的位置**；
3. 展开后的悬浮按钮：**跟着面板上移、浮在面板上方**，再点一次收起；
4. 原操作栏「日志」按钮：**删除**，入口唯一化到悬浮按钮；
5. 自动扩散触发：**开始处理 / 开始下单 / 云文档同步的「确认上传」**；
6. 依赖：**自研**，不引入第三方库。

**实现（前端 9 个文件，后端零改动）**：

- 新增 `frontend/src/lib/reveal.ts` — 纯函数：`cornerRadius`（圆心到四角最大距离）、
  `circleClip`、`openFrames`、`resolveOrigin`、`prefersReducedMotion`、`canAnimate`、
  `pickDockHeight`；
- 新增 `frontend/src/lib/reveal.test.ts` — 31 条 `node --test` 用例（前端测试 53 → 84）；
- 新增 `frontend/src/lib/useLogReveal.ts` — 开合状态机：`origin` + `nonce`，三条触发
  路径（`toggleFrom` / `rememberFrom`+`openRemembered` / `openFrom`）汇成一条展开动作；
- 新增 `frontend/src/lib/useDockHeight.ts` — 实测底部操作栏高度（`[data-task-dock]`，
  取可见最大值；云文档页签没有操作栏时退回 90px 兜底）；
- 新增 `frontend/src/components/LogFab.tsx` — 48px 悬浮按钮，`z-[45]`，
  `bottom = 面板高度 + 操作栏高度 + 10`（永远压在操作栏之上，不会盖住「停止」）；
- `LogConsole.tsx` — 面板展开时用 WAAPI 跑 `clip-path: circle()` 扩散（360ms）；
- `TaskPanel.tsx` — 删掉 `LogToggleButton` 与 `LogToggleProps`，开始按钮点击时
  `rememberFrom(点击点)`，`BottomDock` 加 `data-task-dock`；
- `CloudForm.tsx` — 「确认上传」点击即 `openFrom(点击点)`；
- `App.tsx` — 接线 + 手机端 Toaster 从 `bottom-right` 挪到 `top-center`（避免被悬浮
  按钮压住）+ `main` 的 `padding-bottom` 加 200ms 过渡（表单平滑上移）。

**两个被测试逼出来的关键修正**（值得记住）：

1. **「收起也做圆形动画」是空操作**：面板贴底、把手条就是它最下面一条，而圆心在
   面板上方 —— 把手条的某个角往往**就是整个面板的最远角**（`cornerRadius` 两者
   相等）。所以收起沿用原有的高度下滑过渡，不做圆形动画，也不会有「把手条啪地
   弹出来」。这条是被 `openFrames/closeFrames 互为逆过程` 的断言当场证伪的。
2. **扩散必须让面板「瞬间到目标高度」**：若面板高度照常做 200ms 过渡、同时跑扩散，
   扩散会在面板还没长起来时就把它整个盖住，水波变成一闪而过。因此展开时临时
   `transition-property: none` + 强制回流拿到目标尺寸再量半径（否则量到的是过渡
   起点 34px，半径小一大截、扩散完露不全面板），表单上移交给 `padding-bottom`
   自己的过渡。

**验证**：`pnpm lint` 干净、`pnpm test` **84 passed**、`pnpm build` 成功
（产物 526.0 kB，较改动前 +4.4 kB）、`python -m pytest -q` **929 passed**、
`check_workspace_hygiene.py --working-tree` 通过。**动效仍需真机/浏览器肉眼验收**
（本机无 headless 浏览器，无法截图自证）。

### 第 21 轮（2026-09-16）：用户确认的 3 项决策全部落地

用户答复：「1、a。2、改（但没太理解）。3、改」。前两项（2、3）是**行为变更**，
已明确标注；第 1 项（前端）是**行为不变**的重构。

#### 【行为变更·用户已确认】② `save_wps_config` 部分回传不再重置其它子表

`tables` / `address_order` 原先以**出厂默认**为底（`normalize_*(payload.get(...))`），
于是界面只回传部分子表时，没提到的子表会被**静默重置**成出厂 file_id（第 17 轮实测复现）。
改为 `base=cfg.wps_tables` / `base=cfg.wps_address_order`，与同一方法里其它字段的
「缺键保留原值」白名单语义一致。

**影响面**：界面目前回传**完整列表**，所以正常情况下行为不变；只有「只发改动项」时
才会体现差异 —— 而那正是原来会丢配置的场景。用户点「恢复默认」时界面回传的是默认值
本身，仍然生效。

#### 【行为变更·用户已确认】③ 6 个字段自动去首尾空格

`target_url`、`phone_number`、`sss_account`、`sss_product_name`、`sss_common_address`、
`sss_fixed_address_detail`。其中 `phone_number` 与 `sss_account` 同时是**系统密钥链里的
账号名**：带空格的 `" 138 "` 与 `"138"` 会被当成两个不同账号，导致「密码明明存过却取不到」
且没有任何提示。三个有默认值的字段改为**先 strip 再判空**（纯空白 = 没填 → 回落默认值）；
另外三个没有默认值可回落，去完空格即空串。

#### 【无功能变更】① 前端纯函数抽出为 `src/lib/format.ts` 并加测试

**先撞上一个硬障碍**：Node 的 `node --test` **根本无法 import `.tsx`**（不做 JSX 转译，
实测 `TypeError: Unknown file extension ".tsx"`）。所以「只加 `export`」是**不够的** ——
函数留在组件文件里永远测不到。

**做法**：把 7 个**无 React/DOM 依赖**的纯函数抽到新建的 `frontend/src/lib/format.ts`
（`splitAddressLines`、`isOrderSummary`、`formatLogMsg`、`splitOrderSummary`、
`startOfMonth`、`sameDay`、`formatISO`、`modeError`），4 个组件改为 import。
测试放在 `src/lib/format.test.ts` —— **天然落在既有 glob 里，`package.json` 一个字没改、
也没引入 jsdom 等任何新依赖**。

`statusBadgeMeta` **刻意留在组件内**：它依赖 `statusLabel`，而 `statusLabel` 所在的
`@/hooks/appContext` 用了 `@/` 别名导入，Node 解析不了 —— 搬过去会让 `format.ts`
无法被测试 import。

**行为不变证据**：把搬移后的函数体与搬移前组件里的实现（来自 git HEAD）逐行比对 ——
**代码行完全一致**（唯一差异是 `isOrderSummary` 里一句注释被移到了模块级 JSDoc）。
前端 lint 干净、`pnpm build` 成功、测试 10 → **28 条**。

#### 反证

* 行为变更：把两处改动**逐个改回旧写法**（共 8 个变异），新测试**全部抓到（100%）**。
* 前端测试：**在 5 个时区下各跑一遍**（UTC / Asia/Shanghai / America/New_York /
  Pacific/Kiritimati(UTC+14) / Pacific/Midway(UTC−11)）全部通过。
  过程中抓出一条**我自己写的时区脆弱断言**：原本假定「本地 00:30 与 UTC 必然不同日」，
  但在 America/New_York（UTC−4）两者是**同一天**，测试失败。改为**先算出差异再断言**
  （`if (localParts(instant) !== utcDate)`），任何时区都成立。

#### 验证

全套 **861 passed / 4 skipped**；UTC 与 Pacific/Kiritimati 两时区各跑一遍通过；
ruff、compileall、--self-check、版本一致性、工作区卫生全绿；前端 lint/测试/构建全绿。

### 第 19–20 轮（2026-09-16）：补完「第 5 项剩余工作」

用户指示「4（全仓 ruff format）不做，5（剩余工作）补完」。第 5 项包含三件事，已全部完成：

#### ① README 最后两条安全承诺的测试（`tests/test_wps_cloud_safety.py`，+9 条）

第 11 轮核对时这两条没有针对性测试：

* **「校验不通过则报告并**不更新本地账本**」** —— 实现靠 ``if problems: ... continue``
  跳过 ``ledger.record``。覆盖：校验通过→账本记下该 (日期, 文件) 批次且落盘后可读回；
  校验失败→账本里**没有**该批次；整轮失败→``batches`` 为空；``ledger=None`` 仍合法。
* **「任何失败都只写日志，**不会影响本地排单任务**」** —— 断言实际分层：
  **``apply_plan`` 只吞 ``WpsCloudError``，其它意外异常照抛**（纯云端逻辑层，让编程错误
  暴露更好排查）；真正的安全边界在上一层 **``Bridge.wps_upload`` 的 ``except Exception``**，
  把任何异常转成 ``{"ok": False, "reason": "类型: 消息"}`` 并记一条 ERROR 日志。

顺带补全测试替身：safety 文件的 ``FakeCli`` 原先缺 ``read_formulas``，导致新客户行的
「已出餐/剩余餐」公式回读为空，连**成功路径**都会被判 ``verify_failed``（第 11 轮那批
测试只走失败路径所以一直没暴露）。

#### ② 剩余 7 个 js_api 的测试（`tests/test_bridge_misc_api.py`，30 条）

``choose_excel``（对话框 5 种返回形态 + 后缀校验）、``new_template``（保存名随模式变、
自动补 ``.xlsx``、**取消不算错误**、写盘失败给原因、成功记日志）、``open_external``
（**协议白名单**只放行 http/https）、``begin_window_drag``（非 Linux 直接忽略、GTK 不可用
**绝不抛异常**）、``check_browser`` / ``check_updates``（置状态→起线程→立刻返回、
**防重入守卫**、manual 参数透传）。

记录一处**实测到的方法间不一致**：``choose_excel`` 只认 list/tuple，而 ``new_template``
额外判断了 ``str`` —— 长得像却行为不同，已在测试里写清楚。

#### ③ 公开接口 docstring（**105 → 0**）

先给 ``bridge.py`` 的 22 个 js_api 补（第 20 轮），再给其余 14 个模块补 55 个
**真正的公开接口**（模块级函数 + 类方法）。另外 28 个是**函数内的闭包**（如 ``walk``、
``fetch``、``col_or``），不属于对外接口，**刻意跳过**，避免制造噪声。

写的不是「复述函数名」，而是**契约要点**，例如：``clear_password`` 的「mode 非 "sss"
一律删管理后台那把」、``open_external`` 的协议白名单、``start_order`` 校验失败时的
零副作用、``wps_cloud.record`` 的键格式 ``姓名\u0000电话``、``column_name`` 的 1-based 转换。

**验证（可机械复核）**：把改动过的 15 个文件逐个「剥离所有 docstring 后比对 AST」——
**全部相同**，且 ``git diff`` 的**删除行数合计为 0** —— 纯新增注释，未动一行代码。

#### 反证

* README 承诺：4 个变异最终 **100%**。过程中变异测试抓出一个**我自己制造的覆盖盲点**：
  最初为迁就替身不足，把断言从 ``status == "verify_failed"`` 放宽成 ``!= "ok"``，
  结果「把账本写提到 continue 之前」这个变异**活了下来** —— 因为新客户场景写坏时会
  **更早**以 ``failed`` 退出，根本走不到 ``verify_failed``。改用「老客户 + 关排序」
  直达校验步骤并恢复严格断言后即被抓到。**放宽断言等于悄悄丢掉覆盖。**
* 杂项 js_api：13 个变异全部抓到（**100%**），含去掉外链协议白名单、去掉非 Linux 守卫、
  取消也报错、写盘失败不兜底、不补后缀、不记日志、去掉防重入守卫、manual 没透传。

### 第 18 轮（2026-09-16）：锁住 `wps_preview` 的前置守卫与「测试模式不写记号」

**优化项（工程化规范 / 安全属性回归锁，零产品代码改动）**

`Bridge.wps_preview` 是「云文档同步」的**预览**入口 —— 用户看着它输出的内容决定要不要
真的上传，两个方向都危险：guard 漏了会带着未授权/没选排单表的状态去读云端；guard 搞错
又会把本该能预览的情况挡掉，用户只能盲传。新增 `tests/test_bridge_wps_preview.py`（14 条）：

* **逐个前置守卫**都要返回 `ok=False` + 明确原因，且**绝不抛异常**：没选排单表、
  测试副本没配好、目标表过期、**尚未授权**（提示「去授权」）、读云端出错、
  以及本地 Excel 坏掉这类**意外异常**（要带异常类型，如 `KeyError: 'bad workbook'`）。
  前置条件不满足时**不该去读云端**。
* **安全属性**：测试模式下必须把 `marker_enabled` **强制关掉**
  （`cfg.wps_marker_enabled and not cfg.wps_test_mode`）—— 协作者的通讯记号写在
  **正式表**上，测试模式若把它也写了，等于在别人正式表里留下痕迹。同时验证非测试模式
  下如实透传，防止「恒为 False」这种反向写错。
* **只读保证**：预览**不落账本**（断言 `SyncLedger.save` 从未被调用）。
* 其余透传项：`sort_enabled` / `address_order` 来自配置、`run_date` 用今天、
  `target` 来自配置的时间窗、`test_mode` 如实反映配置、plan 的 warning 以 **WARN**
  级别记日志。

**反证：11 个变异全部抓到（100%）** —— 含两条**致命**的（测试模式也写记号、记号恒为
False）、去掉任一守卫、意外异常不再兜底、透传项被写死、预览去落账本、警告降级成 INFO。

**修改说明**：本轮修掉自己一处断言错误 —— 一开始断言 `test_mode is False`，
但 `AppConfig` 的 `wps_test_mode` **默认为 True**（README：测试模式默认开启），
改为断言「如实反映配置」。

### 第 17 轮（2026-09-16）：锁住设置类 js_api —— 并发现一个**潜在隐患**

**怎么选到这个目标的**

按目标读了本文件；前端那条仍需用户拍板。继续按「静态缺口」推进 `Bridge` 尚未直接
测试的 js_api，本轮取三个与**用户设置**直接相关的入口：`save_wps_config`（33 行）、
`clear_password`、`resolve_decision` / `resolve_captcha`。

**新增 `tests/test_bridge_settings_api.py`（38 条）**

* **`save_wps_config` 的白名单语义**（本轮重点）：它用 `if "x" in payload` 逐个判断，
  所以**没传的键必须原样保留** —— 界面只改一项时绝不能把其余设置清空。
  这里做了**逐键验证**：对 9 个键，每次只传一个，断言其余每个键都保持哨兵值；
  另加「空 payload 什么都不改」。这条一开始写弱了（见下），是变异测试逼出来的。
* `tables` / `test_tables` / `address_order` 会走归一化（去空白、丢掉无 file_id 的条目）；
  其余字符串字段 `strip`、`None`/空串归一成 `""`；写盘 `OSError` → `{"ok": False,
  "reason": "write_failed"}` 而不是抛异常。
* **`clear_password`**：`mode="sss"` 删闪时送那一把、其余（含未知值）走管理后台那一把；
  **账号为空时不去删**（否则可能删到别人的）；两种模式都会写日志且恒返回 `{"ok": True}`。
* **`resolve_decision` / `resolve_captcha`**：把用户在弹窗里的选择交回等待中的任务线程。
  断言投递的值进入 `holder`、**事件被 set**（唤醒 worker）；**同一个 id 只能兑现一次**
  （重复提交/双击第二次必须 `{"ok": False}`）；未知 id 返回 `{"ok": False}` 且不抛；
  非字符串选择会被 `str()`；**唤醒一个 id 不会连带唤醒另一个**；验证码 id 前缀为 `c`。

#### 发现的潜在隐患（既有行为，**未改动**）

`save_wps_config` 里 `tables` 与 `address_order` 走的是
`normalize_*(payload.get(...))`，而 `normalize_*` 在没传 `base` 时以**出厂默认**为底
—— **不是以当前配置为底**。于是只要界面回传的 `tables` 是**部分的**，没提到的子表就会
被**重置成出厂 `file_id`**，用户自定义过的表 ID 被静默丢弃：

```
保存前 : {'东湖中餐': {'file_id': 'CUSTOM_DH'}, '衣锦中餐': {'file_id': 'CUSTOM_YJ'}}
调用   : save_wps_config({'tables': {'东湖中餐': 'NEW_ID'}})
保存后 : {'东湖中餐': {'file_id': 'NEW_ID'},
          '衣锦中餐': {'file_id': 'amqzgcXVMrMWopCdyxkJrxnqzcQ8UbaJG'}, …共 6 张}
```

对比：**同一个方法里其它所有字段用的都是「缺键则保留原值」的白名单语义**，两者并不一致。
当前前端回传的是完整列表（`wps_status()["tables"]`），所以**实际不会触发**；
但若哪天改成「只发改动项」，这里就会丢配置。

**我按「功能不变原则」没有改它** —— 把 `base` 改成 `cfg.wps_tables` 是有意为之的
**行为变更**，需要用户决策。已同时写入「下一步候选」并加了一条测试把这个实际行为
钉住（含注释说明为何不改）。

**反证：12 个变异，最终 100%**

注入白名单被改无条件、字符串不 strip、`tables` 不归一化、`tables` 改以当前配置为底、
写盘失败不报错、布尔字段改严格 `is True`、`clear_password` 两个命名空间对调、去掉空账号
守卫、`resolve_*` 不弹出/不唤醒/恒返回 ok、验证码前缀改成 `d`。

过程中**两次**是「变异脚本或测试自身有问题」而非真缺口，都值得记下：

1. 第一次跑 83%：`return {"ok": False, "reason": "write_failed"}` 这句在 `bridge.py` 里
   出现 **3 次**（`save_order_config` / `save_sss_config` / `save_wps_config`），
   我的变异替换了**第一处**，根本没碰到目标方法 —— 是**变异打偏了**，不是测试缺口。
   加上下文唯一定位后即被抓到。
2. 修完仍是 92%：**我的哨兵值选错了**。布尔字段的哨兵原本取 `False`，而「无条件赋值 +
   键缺失」这种 bug 会算出 `bool(None) == False`，与「保持原值」**结果相同**，
   于是抓不到。把布尔哨兵一律改成 `True`、新值取 `False` 之后，两者才可区分。
   —— 这正是变异测试的价值：**测试「通过」不等于测试「有效」**。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 16 轮**逐字节相同**，
   本轮只新增 `tests/test_bridge_settings_api.py` 并更新本文档（潜在隐患只是**记录**，
   未改代码）。
2. 全套 **800 passed / 4 skipped**（本轮 +38）；UTC 与 Pacific/Kiritimati 两时区各跑
   一遍通过；ruff、compileall、--self-check、版本一致性、工作区卫生、前端 lint + test
   全绿。

### 第 16 轮（2026-09-16）：锁住 `start_order` 的校验与「副作用顺序」

**怎么选到这个目标的**

按目标读了本文件。前端那条仍需用户拍板；`AppConfig` 往返上一轮已做；`credentials` 第
14 轮已做。于是继续按「静态缺口 + 变异测试」推进：`Bridge` 仍有 15 个 js_api 方法没有
直接测试，其中 **`start_order`（48 行）** 最要紧 —— 它是「订单处理」任务的**唯一入口**，
同时承担三件事，每件出错都有实际后果：

1. **表单校验**：校验错了要么让用户无法启动，要么把坏参数放进自动化流程。
2. **副作用顺序**：只有校验**全部通过**才允许写配置、写密钥链、起线程。顺序写反
   （先存密码再校验）会让用户**输错一次就把错密码写进系统密钥链**。
3. **就地更新配置**：方法里明确记着一个历史坑 —— 用「全默认值的新对象」覆盖会把
   **另一半模式（闪时送）的配置重置成默认值**。

**新增 `tests/test_bridge_start_order.py`（36 条）**

* **忙时拒绝**：入口检查 + `_launch` 竞态返回 False，两种都要报 `reason="busy"`。
* **逐字段校验**：`url` / `phone` / `password` 必填（含纯空白）；`excel` 必须存在且后缀
  为 `.xlsx`/`.xlsm`（`.xlsm` 要能通过）；`count` 留空表示「处理全部」、非空必须是
  `1~9999` 的整数（`"0"`/`"-1"`/`"abc"`/`"1.5"`/超上界都拒绝）；`date` 空串**合法**
  （`parse_target_date("")` 返回今天），格式错误才报错。多字段同时错要**一次全报**。
* **失败路径零副作用**（本轮的重点）：校验失败时**不写密钥链、不起线程、不改配置、
  不落盘**，并把状态置为 `error`。变异测试专门验了「把写密钥链提到校验之前」这种
  顺序反转会被抓到。
* **成功路径**：只更新**订单侧**字段（先把闪时送侧设成非默认值，跑完必须原样保留
  —— 直接对应上面那个历史坑）；配置落盘；`remember=True` 时把密码写进密钥链、
  `remember=False` 时**不写**；`_launch` 收到的是**深拷贝快照**（任务跑起来后再改配置
  不影响它），且参数为 `("order", count, password)`；`api_mode` 要带进配置。
* **记录一个实际行为**：payload 里的 `count=None` 会被 `str()` 成字符串 `"None"`，
  于是它不是「留空」而是「非法整数」被拒。前端始终传字符串、实际不会触发，
  但写下来免得后人以为 `None` 等价于留空。

**反证：12 个变异全部抓到（100%）**

包括那条**致命**的「先写密钥链再校验」、`remember=False` 也存密码、传活配置而不是
深拷贝、用新对象整体覆盖配置（会重置闪时送侧）、以及 8 条校验被逐个去掉。

**本轮修掉自己两个测试错误**：① 参数化里放了 `count=None` 却断言「留空」，
实际会被字符串化成 `"None"` 判为非法；② 把 `bridge.status` 当方法调用 ——
它是 **property**。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 15 轮**逐字节相同**，
   本轮只新增 `tests/test_bridge_start_order.py` 并更新本文档。测试全程 monkeypatch
   `set_password` 与 `_launch`，**不碰真实密钥链、不起真线程**。
2. 全套 **762 passed / 4 skipped**（本轮 +36）；UTC 与 Pacific/Kiritimati 两时区各跑
   一遍通过；ruff、compileall、--self-check、版本一致性、工作区卫生、前端 lint + test
   全绿。

### 第 15 轮（2026-09-16）：边界验证 —— 配置空值兜底 / 空白不裁剪 / 往返保真

**怎么选到这个目标的**

按目标先读本文件。候选第 1 条（前端）仍需用户拍板；候选 2 里 `AppConfig.load/save`
经核对**在 `test_models.py` 里已有若干往返测试**，并非真的空白，于是放弃；
`credentials.py` 上一轮已做。

改用一个此前没用过的角度 —— 目标里点名的**边界验证**：写模糊测试脚本，对
「归一化类纯函数」做**幂等性**与**不变量**检查（归一化跑两遍必须等于跑一遍，
否则程序反复运行/反复读写配置会让值**持续漂移**）。

**模糊测试结果：没有发现缺陷，但发现了一组反直觉的既有行为**

* **幂等性全部成立**（各 3000 例随机输入）：`_address_key`、`canonical_address`、
  `address_group`、`normalise_phone`、`split_text_and_number` 的两半、
  `clamp_split_ratio`；三个 `normalize_wps_*` 各 1000 例也全部幂等。
* **不变量成立**：`clamp_split_ratio` 恒落在 `[0.30, 0.55]`；`_address_key` 结果不含空白；
  `person_key` 的电话部分恒为纯数字或空。
* **但 300 组随机 `save → load` 往返（8700 次字段比对）有 5 个字段对不上**。逐条核对
  源码后确认**都不是缺陷，而是 `AppConfig.__init__` 里有意的 falsy 兜底**
  （`x or "默认值"`）：`sss_product_name`(轻食) / `sss_common_address`(嗯哼) /
  `sss_store_name`(一口轻食) / `sss_fixed_area_code`(330110) /
  `sss_fixed_address_detail`(浙江农林大学东湖校区)。

**这类行为虽然「正确」，但足够反直觉**（用户把「商品名称」清空保存，重启后会**变回
「轻食」**），因此新增 `tests/test_config_boundaries.py`（46 条）把实际语义钉死，
免得日后有人「顺手改成保留空值」而悄悄改变既定行为：

* **空值 → 默认值**：逐一覆盖上面 5 个字段的 `""` / `None` / 正常值三种输入，
  并额外验证「清空后存盘再读回仍是默认值」。
* **`sss_order_source` 只认 `excel`**：`"EXCEL"` / `" excel "` / `"Excel"` 都归一成
  `"excel"`，而 `"excel2"` / `""` / `None` / `"e"` **一律退回 `"wps"`** ——
  坏值绝不能让程序从「读云端」变成「读本地 Excel」。
* **空白不被裁剪（记录实际行为）**：`sss_product_name`、`sss_common_address`、
  `sss_fixed_address_detail`、`phone_number`、`target_url` 都**只做 falsy 判断、不做
  strip**，所以 `"   "` 是「有值」的会被原样保留。副作用值得知道：`phone_number`
  同时是系统密钥链的账号名，`" 138 "` 与 `"138"` 会被当成**两个不同的账号**去取密码。
  这不是缺陷（唯一写入口是界面输入框），但写下来免得后人误以为这里有去空白保护。
* **对照组**：`order_date` 是**唯一**会 strip 的字段（`str(x or "").strip()`），
  把这处**不一致**也钉住，说明裁剪与否是按字段各写的、不是统一规则。
* **数值夹紧**：`sss_max_workers` 夹到 `[1, 20]`、`sss_read_timeout_s` 夹到 `[1.0, 120.0]`、
  `split_ratio` 构造时就夹紧。
* **往返保真**：28 个字段用**合法取值**存取一轮后逐字段相等；`order_count=None`
  （表示「全部订单」）不能被当成 0 或丢失。
* **归一化不变式**：三个 `normalize_wps_*` 对脏输入的幂等性、**不得就地修改入参**、
  出厂地址顺序两次调用结果一致。

**反证：12 个变异，11 个直接抓到，第 12 个经核实由第 4 轮的文件覆盖**

注入 12 个真实 bug：`sss_order_source` 不校验 / 默认改成 excel、5 个空值兜底逐个去掉、
`order_date` 不再 strip、两处数值夹紧放宽、`split_ratio` 不夹紧、`normalize_wps_tables`
去掉 file_id 过滤。**前 11 个全部抓到**；第 12 个（去掉 file_id 过滤）本文件抓不到
（幂等性对它不敏感），但**实测由第 4 轮的 `test_config_normalization.py` 抓到
（2 条失败）** —— 已把这条跨文件的分工写进测试文件，避免后人误以为本文件管了全部。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 14 轮**逐字节相同**，
   本轮只新增 `tests/test_config_boundaries.py` 并更新本文档。
2. 全套 **726 passed / 4 skipped**（本轮 +46）；UTC 与 Pacific/Kiritimati 两时区各跑
   一遍通过；ruff、compileall、--self-check、版本一致性、工作区卫生、前端 lint + test
   全绿。
3. **本轮未做真机验证的说明**：以上全是本地纯函数与配置文件读写的单元测试，
   不碰云端、不碰真实密钥链；但 `phone_number` 空白不裁剪这条若真的发生，会影响
   密钥链取密码，值得用户在真机上留意（见硬约束第 4 条）。

### 第 14 轮（2026-09-16）：锁住凭据命名空间隔离（管理后台密码 vs 闪时送密码）

**怎么选到这个目标的**

按目标先读本文件。下一步候选第 1 条（前端测试缺口）仍卡在需用户拍板的两个选项上，
用户已连续三轮未答复，不擅自动；候选第 2 条里 `app/credentials.py` 是**唯一 9/9
公开函数全部零覆盖**的模块，且守的是一条 README 明确写下的安全属性：

> 闪时送密码使用独立凭据名 `yikou-light-food-sss`，与管理后台账号密码**互不覆盖**。

**为什么这条必须钉死**：用户用**同一个手机号**登录管理后台与闪时送两个平台。一旦
`set_sss_password` 误用了 `SERVICE_NAME`，后存的密码会**悄悄覆盖**先存的 —— 表现是
「密码明明没改，却登录失败」，而且不会有任何报错，极难排查。这满足硬约束 4.1
的「有没有必要」。

**新增 `tests/test_credentials.py`（16 条）**

* **命名空间隔离（核心）**：`SERVICE_NAME` 与 `SSS_SERVICE_NAME` 必须不同；
  同名账号各存一把密码后**两把都读得回来**（`len(keyring.data) == 2`）；
  **删掉闪时送密码不能连带删掉管理后台密码**。
* 三组入口（get/set/delete × 管理后台/闪时送）各自打到**正确的服务名**上，逐一断言
  传给密钥链的参数。
* 空账号（`""`）**不调用密钥链**，直接返回 `None`/`False`。
* **记录一个实际行为**：判断条件是 `not username`，所以**纯空白会被当成有效账号**
  传给密钥链。这不是缺陷（界面不会传空白），但写下来免得后人误以为这里有去空白保护。
* **优雅降级**：`keyring` 是可选依赖。读/写/删各自抛异常时分别返回 `None`/`False`/`False`，
  **绝不向上抛**；`_backend()` **自身**把 import 包在 try 里（用 `builtins.__import__`
  打桩让 `import keyring` 失败，验证它返回 `None` 而不是抛）。
* `prompt_password` 的提示语（含 `for <账号>` 与不带账号两种）、以及 Tkinter 层用过的
  旧别名 `load_password` / `save_password` 仍等价且支持自定义 service。

**反证：10 个变异全部抓到（100%）**

包括那条**致命**的「两种凭据用同一个服务名」、三个 `*_sss_*` 入口分别误用默认服务名、
空账号守卫被去掉、三处 try/except 被去掉或改成失败也报成功、`set_password` 不把密码
传下去、以及 `load_password` 别名接错函数。

**本轮修掉自己的三个测试错误（记下来）**

1. 参数化时写 `raise_on=("get",)` 却断言 set/delete 也失败 —— 实际只有 get 会抛，
   set/delete 正常返回 `True`。拆成三条独立测试。
2. 我原以为 `_backend()` 抛异常会被吞掉，实测**会向上抛** —— 因为调用方没包 try。
   但真实场景下 `_backend()` 自己已经把 import 包在 try 里、永远不会抛，所以这是
   测试假设错了而非缺陷。改为直接用 `builtins.__import__` 打桩验证 `_backend()`
   的 import 守卫。
3. `prompt_password` 的提示语是 `"Password: "`（**带尾随空格**），我按 `"Password:"` 写，
   实测报错后发现是断言写错。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 13 轮**逐字节相同**，
   本轮只新增 `tests/test_credentials.py` 并更新本文档；测试全程 monkeypatch
   `_backend`，**不触碰真实系统密钥链**。
2. 全套 **680 passed / 4 skipped**（本轮 +16）；UTC 与 Pacific/Kiritimati 两时区各跑
   一遍通过；ruff、compileall、--self-check、版本一致性、工作区卫生、前端 lint + test
   全绿。

### 第 13 轮（2026-09-16）：补上前端 `lib/theme.ts` 的测试（零配置改动）

**怎么选到这个目标的**

按目标要求先读本文件。下一步候选第 1 条「前端测试缺口」仍卡在需要用户拍板的选择上
（给组件内纯函数加 `export`，或引入 jsdom + React 测试库，**两者都超出「零产品代码
改动」**），用户尚未答复，因此不擅自动。

于是重新在 `frontend/src/lib/` 里找**已经导出、因此无需任何改动就能测**的模块。发现
`theme.ts`（27 行）是 `src/lib/` 下唯一没有测试的模块，而 `pnpm test` 的 glob 是
`node --test src/lib/*.test.ts` —— 它**天然就在 glob 覆盖范围内**，连测试脚本都不用改
（对比之下 `src/hooks/appContext.ts` 虽然也有个纯函数 `statusLabel`，但它在 `src/hooks/`
下，要测就得改 `package.json` 的 glob，那才是产品代码改动，故不做）。

**为什么主题值得测**：主题坏了**只影响观感、不影响功能**，所以很难被察觉 —— 界面照常跑，
只是每次重启都回到浅色、或深色下文字看不清。而这段逻辑恰好有三处容易写错的边界：

1. `initialTheme` 必须**严格校验**存下来的值（只认 `"dark"` / `"light"`）。宽松写法
   （`return saved`）会把被写坏的 `"DARK"` / `"purple"` / 空串当成合法主题返回。
2. localStorage 在**隐私模式 / 存储被禁用**时会抛异常，两处都必须吞掉：读失败退回浅色，
   写失败不能影响切换本身、更不能把异常抛给调用方。
3. `applyTheme` 必须**双向**切换 `.dark`（切回浅色要移除），不能只加不减。

**做法**：新增 `frontend/src/lib/theme.test.ts`（8 条），沿用既有
`bridge.events*.test.ts` 的做法 —— 给 `globalThis.localStorage` / `globalThis.document`
打桩，**不需要 DOM 环境、不新增任何依赖**。先跑探查脚本确认真实行为（含 `getItem` 抛错、
`setItem` 抛错、大写 `DARK`、垃圾值等），再照实写断言。前端测试 2 → **10 条**。

**反证：7 个变异，6 个抓到，1 个是等价变异**

| 注入的 bug | 结果 |
|---|---|
| `initialTheme` 不再严格校验（写坏的值也返回） | ✅ 抓到 |
| `initialTheme` 默认主题改成深色 | ✅ 抓到 |
| `initialTheme` 读 localStorage 不再吞异常 | ✅ 抓到 |
| `applyTheme` 只加不减（切不回浅色） | ✅ 抓到 |
| `applyTheme` 不再持久化 | ✅ 抓到 |
| `applyTheme` 写失败不再吞异常 | ✅ 抓到 |
| `initialTheme` 弱化成「只认 dark，不认 light」 | ⚪ **等价变异** —— 存 `"light"` 时会落到末尾的 `return "light"`，结果完全相同；已在测试里注明，避免后人误判为缺口 |

即 **6/6 有效变异全中**。


**CI 抓到的第二个「自己写的测试不稳」案例（重要）**

本轮推送后 **macOS 3.13 作业失败**（Windows 3.13 被 fail-fast 取消），根因是
**第 2 轮写的 `test_concurrency_is_faster_than_serial` 本身是墙钟抖动测试**：

```
AssertionError: 串行 0.473s / 并发 0.404s
assert 0.40363208299999087 < (0.4733637500000043 * 0.75)
```

即原断言「并发实测 < 串行实测 × 0.75」在 macOS runner 上被**线程启动开销**吃掉收益
（只到 1.17x），而本机跑一直是 2x —— 典型的「本地全绿、CI 假失败」。**墙钟比值天生
不稳，不能作为断言**。

**第一次修法（不够）**：改成与「`N × delay` 理论下界」比较而不是比两次实测。
**CI 上仍然假失败** —— 日志显示 macOS 上 10 次读 / 4 并发跑了 **0.579s**，
而下界只有 0.300s；18 次读 / 2 并发跑了 1.193s，下界 0.540s。也就是说
**macOS runner 上 `time.sleep(0.03)` 实际要花 60~130ms**，连 sleep 时长本身都不保证。

**最终修法**：**彻底删掉两处墙钟断言**，并发的证据只保留**确定性**指标 ——
`test_reads_actually_overlap` 断言「同一时刻在途请求数 `max_inflight` 在 2 与并发度
之间」（先钉住总读数，免得测试名不副实）。另把 `import time` 的用法限定在
FakeCli 的 `sleep` 里（制造重叠），不再用于断言。副作用：这两个文件整体快了 0.4s。

**反证（第一次做错了，记下来）**：一开始把模块常量 `WPS_COPY_CHECK_WORKERS`
从 2 改成 1 去验，结果**测试照样通过** —— 因为那两条测试会 **monkeypatch** 并发度，
常量根本不参与；而 1 本来就在 `test_default_workers_stay_conservative_for_rate_limit`
允许的 `1..2` 区间内。改成**在函数体内强制 `workers = 1`**（真正取消并发）后，
两个 `test_reads_actually_overlap` **双双失败** —— 说明「确实重叠」这条证据
在去掉计时之后依然有效。

**无功能变更 —— 证据**
1. 既有前端源文件（`theme.ts` / `bridge.ts`）与 `frontend/package.json` **逐字节未改**
   —— 本次只**新增**一个测试文件，且它天然落在既有 glob 里；`app/` 同样零改动。
2. Python 全套 **666 passed / 4 skipped**（UTC 与 Pacific/Kiritimati 两时区各跑一遍通过）；
   前端 **10 passed / 0 fail**；ruff、compileall、--self-check、版本一致性、
   工作区卫生、前端 lint 全绿。

### 第 12 轮（2026-09-16）：新增前后端契约测试（TS 接口 ↔ Python 返回值）

**怎么选到这个目标的**

按目标新要求先读了本文件：硬约束 1「功能不变」、约束 4「改前先问必要性」，
下一步候选第 1 条是前端测试缺口，但它卡在需要用户拍板的两个选项上
（给组件内纯函数加 `export`，或引入 jsdom + React 测试库，**两者都超出「零产品代码改动」**），
因此本轮不做。

改从「零改动但价值高」的角度找一个尚未覆盖的风险点：**前后端契约漂移**。
`frontend/src/lib/bridge.ts` 用 TS 接口描述 Python 的返回结构，但两边是**不同语言、
没有编译期或运行期链接**：

* Python 改了返回字段名（如 `weekday_number` → `marker_weekday`），`pytest` **全绿**
  —— Python 测试断言的还是新名字；
* 前端 `pnpm test` 也**全绿** —— 那 2 条测试只覆盖事件游标；
* 结果界面读到 `undefined`，**静默失灵**，只有人肉点开才发现。

这正符合约束 4.1「有没有必要」：现有 666 条测试里，**没有任何一条能发现这种漂移**。

**做法**：新增 `tests/test_frontend_contract.py`（9 条）。从 `bridge.ts` 解析出接口字段，
再调用真实的 Python 生产函数，断言 **TS 标记为必需（无 `?`）的字段一个都不能少**。
用正则而不是 TS 编译器 —— 这些接口写法规整，正则足够，且**不给项目引入新依赖**
（这也是不选 jsdom 路线的原因）。Python 多返回字段是无害的（TS 会忽略），故只做单向校验。

覆盖 6 组契约：`AppState` / `AppConfigState` ← `bridge_ready()`；
`WpsStatus` / `WpsTableState` ← `wps_status()`（含 **CLI 缺失分支**也要满足契约）；
`WpsCopyCheck` / `WpsCopyCheckItem` ← `wps_check_copies()`（含失败返回形状）。

**当前状态：无漂移**。探测确认 6 个接口的必需字段 Python **全部返回**，且没有 TS
未声明的多余字段。所以这是一条**回归护栏**，不是今天的 bug 修复。

**反证：8 个契约变异，6 个直接抓到，另 2 个经核实已被别处覆盖**

| 注入的漂移 | 结果 |
|---|---|
| `AppConfigState`：去掉 `wps_marker_enabled` | ✅ 抓到 |
| `AppConfigState`：`wps_marker_enabled` → `marker_on` | ✅ 抓到 |
| `WpsStatus`：去掉 `ok` | ✅ 抓到 |
| `WpsTableState`：去掉 `last_people` | ✅ 抓到 |
| `WpsTableState`：`last_people` → `people_count` | ✅ 抓到 |
| `WpsCopyCheckItem`：去掉 `status` | ✅ 抓到 |
| `AppState`：去掉 `event_producer_id` | ⚪ 等价变异 —— 该字段在 TS 里是 `event_producer_id?`（**可选**），去掉后前端仍能正常工作；且第 6 轮的测试已断言它非空 |
| `WpsStatus`：把 `weekday_number` 的赋值改写到别的键 | ⚪ 键仍在（初始字典第 958 行已置 `0`），本文件只查存在性；**该变异被第 6 轮的 `test_wps_status_target_date_and_tables` 抓到**（已实测） |

即 **6/6 有效变异全中**；两个「漏杀」一个等价、一个由别处的值断言兜住。
据此在测试文件里写明了本文件的边界：**本文件只防「字段名漂移」，值算错由
`test_bridge_api_surface.py` 负责** —— 避免后人误以为它更强。

另加一条**解析器自检**（`test_interface_parser_reads_real_definitions`）：断言解析
出的字段数量与必需/可选区分正确，且不存在的接口会抛错 —— 否则正则一旦失效，
上面几条会「假通过」。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 11 轮**逐字节相同**，
   本轮只新增 `tests/test_frontend_contract.py` 并更新本文档。
2. 全套 **666 passed / 4 skipped**（本轮 +9）；UTC 与 Pacific/Kiritimati 两时区各跑
   一遍通过；ruff、compileall、--self-check、版本一致性、工作区卫生、
   前端 lint + test 全绿。

### 第 11 轮（2026-09-16）：锁住 README「安全边界」的三条承诺（回滚 / 不回滚）

**怎么选到这个目标的（硬约束 4.1「先问有没有必要」）**

本轮的出发点是补「可维护性」这一维度（前几轮集中在执行效率与工程化规范）。先做了两项
体检，**结果都否掉了原计划**：

* 重复代码扫描（跨函数 ≥6 行的语句块）：**0 组** —— 代码库本身已经拆得很干净，
  「消除重复」无事可做。
* 公开函数缺 docstring 105 个、缺类型标注 7 个 —— 但给 105 个函数批量补注释属于
  「看起来更规范」，**信息密度低、收益边际**，不满足「有没有必要」这一问，因此没做。

于是换一个角度问：**这个项目里最不能出错的是什么？** 答案是写**协作者维护的正式
云端排单表**。README 的「安全边界」一节对这类操作给了 6 条明确承诺。逐条核对测试
覆盖后发现：

| README 承诺 | 改动前的测试覆盖 |
|---|---|
| 找不到目标日期列 → 不写不建列，只提示 | 有 |
| 写入后逐格回读校验（含"这个行号上确实是这个人"） | 有（`test_apply_plan_detects_silent_write_failure` 等） |
| **排序失败 → 把插进去的新行整块删掉，云端恢复原状** | **0 —— `_rollback_inserts` 被调用两次却没有任何测试** |
| **排序成功之后的一步失败则不再删行**（否则会删错人） | **0** |
| 校验不通过则不更新本地账本 | 0（仅账本自身读写有测试） |
| 任何失败都只写日志，不影响本地排单任务 | 0 |

即：**风险最高、且承诺写得最明确的两条回滚语义，恰恰完全没有测试**。这才是真正
「有必要」的改动，于是新增 `tests/test_wps_cloud_safety.py`（6 条）。

**三条被钉死的语义**

1. **回滚必须从后往前删**。`_rollback_inserts` 用
   `sorted(inserted, reverse=True)`：先删靠前的插入块会把后面块的行号整体上移，
   删的就不是原来那些行了。测试同时断言删除顺序、空 `inserted` 时不发任何调用、
   **删除失败时告警并停手（不抛异常，免得盖掉原始错误）**、成功时给出
   「已回滚 N 处插入，云端恢复原状」。
2. **新客户行写入失败 → 回滚**。断言 `deleted_rows == inserts`（插入的块被原样删掉）、
   `failed == 1`、并且**网格里只剩原来的两位老客户**（真的恢复原状，不只是调了接口）。
3. **排序失败 → 回滚；排序成功之后的失败 → 绝不回滚**。后一条与第 2 条**方向相反**，
   最容易写反：排序一旦生效，新行已散落到各地址组，此时「按插入位置整块删」删的是
   别人。测试断言此时 `deleted_rows == []`、状态为 failed、面向用户的日志含
   **「重新上传，重复执行安全」**，且新行信息仍留在云端等用户重传。

**反证：7 个变异全部被抓（100%）**

往实现里注入 7 个真实 bug，看新测试能否 FAIL：删除顺序 `reverse` 翻转、删除失败后
改成 `continue`、去掉空值提前返回、去掉成功提示、写入失败不回滚、排序失败不回滚，
以及**最危险的「排序成功之后也去回滚」**。全部抓到 —— 包括最后那条会把别人的行删掉的
反转。

**本轮踩到的两个自身问题（都已修）**

* 失败注入点最初放在 `sort_range` 里，但真实时序是
  `sort_range → delete_columns → read_person_rows`，而 `delete_columns` 会把标志复位，
  导致注入根本没触发（测试却「通过」了）。改到 `delete_columns` 之后才真正命中。
* 一条断言写的是「reason 里含『重新上传』」，实际 reason 文案是「排序后回读失败：…」，
  「重新上传」在**日志**里而不是 reason 里。已改成分别断言 reason 与日志。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 10 轮**逐字节相同**，
   本轮只新增 `tests/test_wps_cloud_safety.py` 并更新本文档。
2. 全套 **657 passed / 4 skipped**（本轮 +6）；UTC 与 Pacific/Kiritimati 两个时区下
   各跑一遍通过；ruff、compileall、--self-check、版本一致性、工作区卫生全绿。
3. 本轮未做真机验证的说明：这些测试全部用内存假 CLI 驱动，**不碰真实云端**；
   回滚语义在真实 WPS 上的表现仍需用户在真机复核（见硬约束第 4 条）。

### 第 10 轮（2026-09-16）：按新硬约束复核第 4、5 轮的测试改动

**背景**：用户要求「这轮结束之后再根据我的新要求把前两轮的修改再做好验证」——
即用第 6 轮新增的**硬约束第 4 条**（改前考察、改后验证可行性、先想衍生 bug、做好反证）
回头复核第 4、5 轮那批**纯测试改动**（共 8 个文件、944 行新增）。这两轮当时只做了
「变异测试」，没有检查平台差异、时区依赖、密闭性与顺序依赖。

复核范围：`git diff 1be7bdf..0ede70c -- tests/` 的 8 个文件
（`test_config_normalization` / `test_api_client` / `test_automation_parsing` /
`test_processing` / `test_sss` / `test_webview_app` / `test_address_aliases` /
`test_wps_cloud_build_plan_concurrency`）。

#### 一、机械审计（只审新增行，不误报老代码）

写脚本把 diff 里**新增的行**抽出来，逐条正则匹配七类可疑写法：

| 检查项 | 命中 |
|---|---|
| ① 写死 POSIX 路径（`/tmp`、`/home`、`/dev`…） | **0** |
| ② 平台分支假设（`os.name` / `sys.platform`） | **0** |
| ③ 依赖当前时间/时区（`datetime.now()` / `date.today()` / `.weekday()`） | **0** |
| ④ 可能不密闭（`user_data_dir` / 密钥链 / 默认账本路径） | 2 → 见下 |
| ⑤ 文本写入后按字节比对（Windows 换行坑，第 1 轮踩过） | **0** |
| ⑥ 弱断言（只查 `is not None` / `isinstance`） | **0** |
| ⑦ 打开文件后可能被 Windows 锁住（`r+b` / `truncate`） | **0** |

④ 的 2 处是 `tests/test_address_aliases.py` 调 `aliases_path()` / `pending_path()`。
核实结论：**无害** —— 这两个函数只是 `user_data_dir() / "xxx.json"` 的 Path 运算，
**不读不写不建目录**（会 `mkdir` 的是 `load_aliases()`，测试没调它），断言也只用
`.name` 与 `.parent` 比较。

#### 二、时区扫描（本地就能做的跨环境验证）

同一套测试在 5 个时区下各跑一遍，**全部 650 passed / 4 skipped**：

```
TZ=UTC                650 passed      TZ=Pacific/Kiritimati  650 passed   (UTC+14)
TZ=Asia/Shanghai      650 passed      TZ=Pacific/Midway      650 passed   (UTC-11)
TZ=America/New_York   650 passed
```

日期相关的那批测试（`expected_delivery_date` 的 16 点顺延、`get_weekday_fill_value`
的「次日周几」、`wps_status` 的 `weekday_number`）**都显式传入 `now`**，因此不依赖
机器时区 —— 这正是它们能跨时区通过的原因。

#### 三、抖动 / 隔离性 / 顺序依赖

* 全套**连跑 3 轮**：650 passed × 3，无抖动。
* 5 个新增测试文件**各自单独运行**：71 / 39 / 35 / 38 / 16 全绿，互不依赖。
* 把 4 个新增文件**按正序与逆序**各跑一遍：都是 183 passed —— 无顺序依赖。

#### 四、反证：自动变异扫描（并修好了扫描器自身的两个 bug）

手挑变异覆盖面有限，于是写了**自动变异扫描器**：用 AST 定位被覆盖的函数，逐行翻转
比较运算符（`==`↔`!=`、`<`↔`>=`…）、翻转 `and`/`or`、把 `return <expr>` 打桩成
`return None`，逐个应用后跑对应测试文件并统计。

**扫描器 v1 有两个会「虚高分数」的缺陷，已修**：

1. 它会去变 **docstring / 注释 / 类型标注 / 循环语法** —— 例如把 `-> bool` 改成
   `-<= bool`、把 `for keyword in ...` 改成 `for keyword not in ...`，产生**语法错误**；
   而语法错误让 pytest 返回码非 0，被 v1 当成「抓到」。v1 报的 95%（83/87）里就混着
   这种假阳性。
2. `excluded_lines` 跳过 def 签名时**越界把单行函数体也跳掉了**，导致 `worker_alive`
   这类单行函数一个变异都没生成（显示「有效 0」）。

**v2 的修正**：用 AST 排除 docstring 与 def 签名行、跳过纯注释行、只做**语义上仍然
合法**的变异，并把结果分为**抓到 / 漏杀 / 无效**三类，无效的剔除不计分。

**v2 结果：有效变异 51 个，抓到 51 个，漏杀 0，无效 0 → 100%。**
覆盖 24 个函数（含 `compute_delivery_time`、`should_skip_address`、`worker_alive`
等单行函数）。

#### 五、v1 扫描暴露的一个真缺口（已补）

v1 曾报 `extract_product_note_text` 有一个存活变异：**注入浏览器的 JS 被改坏也测不出来**。
原因是我的假 `_Page` **完全不看传给它的 JS**，只按 CSS 返回数据 —— 所以那段 JS 里
`td:nth-child(1)`（品名列）被改成别的列号，测试照样通过。

补法：让假 `_Page` 记录注入的 JS，并断言它**必须去读第 1 列（品名）且取 `innerText`**。
这是**契约断言**（钉住它依赖的列号），不是复述实现。
验证有效：把 `extract_product_note_text` 内的 `td:nth-child(1)` 改成
`td:nth-child(2)`，该测试**立即失败**（同时确认了改 `MEAL_ROWS_JS` 里那处同名选择器
**不会**误伤这条测试，两者是独立的）。

#### 结论

第 4、5 轮的测试改动在**平台差异、时区依赖、密闭性、顺序依赖、断言强度**五个维度上
复核通过；期间发现的问题**全部出在我自己的验证工具与测试自身**，**产品代码依旧一行
未改**。本轮另修好自动变异扫描器的两个缺陷，并补了 1 条 JS 契约断言（650 → 651 条）。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 6 轮**逐字节相同**，
   本轮只改 `tests/test_automation_parsing.py` 与本文档。
2. 全套 **651 passed / 4 skipped**，并在 UTC / Asia/Shanghai / Pacific/Kiritimati
   三个时区下各跑一遍全部通过；ruff、compileall、--self-check、版本一致性、
   工作区卫生全绿。

### 第 6 轮（2026-09-16）：补齐 `Bridge` 的 js_api 面测试 + 变异测试 95%

**优化项（工程化规范 / 可维护性，零产品代码改动）**

`Bridge` 的公开方法就是**前端能调用的全部接口**（pywebview 把它们暴露成 JS
Promise）。改动前 `tests/` 只碰过其中 11 个，剩下 26 个没有任何直接测试。本轮先覆盖
其中**不依赖浏览器与网络**的一批，新增 `tests/test_bridge_api_surface.py`（38 条）：

* **`bridge_ready`** —— 前端握手，返回 40+ 字段的初始状态（前端 `AppState` 的镜像），
  字段缺失或改名会让界面**静默失灵**。覆盖：version / status / frozen /
  `event_producer_id`（前端靠它识别 Python 进程重启，不能为空）、**逐个断言前端
  需要的全部 config 键**、路径类字段必须是字符串（`None` 会渲染成 "null"）、
  两个密码槽位**各自装对**（用 monkeypatch 让 `get_password` / `get_sss_password`
  返回可区分的值，否则「槽位对调」这种 bug 也能被空串蒙混过关）、`wps_tables`
  是 `dict()` **浅**拷贝（外层新、内层共享 —— 这里无害，因为 pywebview 会把返回值
  JSON 序列化后再交给 JS，测试里把这个实际行为写清楚，免得后人误以为是深拷贝）、
  以及两次调用之间能反映配置变化。
* **`wps_status`** —— 云同步状态。其中最要紧的是 `writing_test_copies`：它判断
  「当前实际写入的是测试副本还是正式表」，**判错会让用户以为在安全测试、实际却在
  改协作者的正式表**。覆盖：生效目标是副本 → True、生效目标就是正式表 → False、
  一张生效表都没有 → False（不能误报成「正在写副本」）、返回结构完整、
  `weekday_number` 用的是**运行日**而不是目标日期（文档记录的坑）、
  `address_order_defaults` 每次都是新的、找不到 kdocs-cli 时给 `reason` 而不抛异常、
  账本里有批次时带出 `last_sync` / `last_people`（以及没有批次时为空）。
* **`frontend_report` / `pop_reports`** —— 前端回传运行快照的通道（自动化验收依赖它
  而非 `evaluate_js`）。覆盖：dict 与字符串都能收、**取出即清空**、**只保留最新
  50 条**（扔 60 条后剩下的正是第 11~60 条）。
* **`worker_alive`** —— 无 worker 时 False、随线程状态变化。
* **`set_split_ratio`** —— 夹紧到 `[0.30, 0.55]`、NaN/非数值回退 0.38、
  **落盘后重新加载仍是夹紧值**。
* **`window_action`** —— 没有窗口时 `{"ok": False}`、minimize、未知动作仍返回 ok、
  `toggle_maximize` **成对交替**（pywebview 没有「是否最大化」查询，只能靠
  `_maximized` 自己记）、`close` 转交 `request_close`。
* **`restore_wps_production_tables`** —— 没有备份时明确报错、成功时切回正式表并
  **必须以 WARN 级别留痕**（这是危险动作，降级成 INFO 会被测试抓住）。
* **`on_native_closing`** —— 已在关闭中或无任务 → 放行（True）；任务在跑 → 取消原生
  关闭（False）并**另起线程**走 `request_close`，不在 pywebview 回调里阻塞。
* **`echo_test`** —— 原样回显 message，`payload_keys` **排序后**返回，非 dict 返回
  None 且不崩。

**用变异测试验收（沿用第 4/5 轮方法论）**

在 `/tmp` 副本上注入 **19 个真实 bug**。首轮 84%（16/19，其中一次运行出现的
`writing_test_copies` 漏杀经手工复现确认是**脚本自身的瞬时假象**，复跑即被抓到），
2 个存活变异是**真缺口**并已补：

| 存活变异 | 为什么没抓到 | 补的断言 |
|---|---|---|
| `excel_path` 永远返回空串 | 原断言只查 `isinstance(..., str)`，空串也是 str | 设置真实路径，断言返回**字符串形式**的路径（含空格与中文） |
| 两个密码槽位对调 | 未配置账号时 `order` 与 `sss` 都是 `""`，对调等价 | monkeypatch 两个取密码函数返回**可区分**的值，断言各自装对 |

复测得分 **95%（18/19）**。唯一存活的变异（`address_order_defaults` 直接返回
`default_wps_address_order()`）经核对是**等价变异**：该函数本来就每次返回新的 dict
与新的 list，再包一层推导式并不改变行为 —— 已在测试里写明，避免后人误判为缺口。

**本轮未做真机验证的说明**：以上全部是**只读**状态聚合与本地配置写入的单元测试，
不涉及云端读写、不涉及真实下单，因此不需要登录站点即可确认。但凡后续要动
`wps_status` 里与云端有关的字段或 `Bridge` 的任务启动方法，都需要用户在真机上复核
（见「硬约束」第 4 条）。

**CI 抓到一个「自己刚写下的约束自己就违反」的案例（值得记住）**

本轮把「注意 Windows 与 Linux 差异」写进硬约束后，推送上去 **Windows 作业立刻失败**：
新测试 `test_bridge_ready_stringifies_excel_paths` 里写死了 POSIX 路径
`Path("/tmp/排单 名单.xlsx")` 并断言等于同一字面量；Linux/macOS 全绿，而 Windows 上
`str(Path("/tmp/x"))` 是 `"\tmp\x"`，断言必然失败。
修复：改用 `tmp_path` 现算期望值（`assert config["excel_path"] == str(excel)`），
并顺手审计了本轮与第 4、5 轮所有新增测试里的硬编码路径 —— 只此一处，其余用的是
`tmp_path` / `.name` / `.endswith`，本身平台无关。**单测在本地全绿 ≠ 在三个平台都绿。**

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 5 轮**逐字节相同**，
   本轮只新增 `tests/test_bridge_api_surface.py`。
2. 变异测试全程在 `/tmp/mut6` 副本进行，已核实副本清理、真实仓库未被污染。
3. 全套 **650 passed / 4 skipped**（本轮 +38 条）；ruff、compileall、--self-check、
   版本一致性、工作区卫生、前端 lint + test 全绿。

### 第 5 轮（2026-09-16）：补齐 api_client / automation 等模块的纯函数缺口 + 变异测试 100%

**优化项（工程化规范 / 可维护性，零产品代码改动）**

第 4 轮建的「静态覆盖率代理」脚本本身有 bug（正则 `\b` 在 `_` 后不匹配、
漏掉 `obj.func(` 形式、漏掉 `import x as y` 别名），导致误判。本轮先把脚本修好，
并用第 4 轮**已知已覆盖**的 `normalise_phone` / `clamp_split_ratio` 做回归校验，
确认脚本可信后才得出缺口清单。**真实缺口 10 个**（`normalize_delivery_point` 经查是
**假缺口** —— 它在 `tests/test_delivery_point.py` 里以 `ndp` 别名被 50+ 组用例覆盖）。

新增/补充测试覆盖以下函数（改动前 `tests/` 里**一次都没引用过**）：

* **`app/api_client.py`**（新建 `tests/test_api_client.py`，39 条）
  - `is_auth_expired_payload`：平台用 **HTTP 200 + `code=10000`** 表示登录失效，
    只看状态码不够。判错会**无限重登**或**静默失败**。覆盖：错误码 `{401,10000}`
    的整数与字符串形式、`code` 转不成整数时不崩也不误判、**每条消息关键词逐个验证**
    （少一个就可能漏判）、大小写不敏感、`msg` 作为 `message` 的别名、非 dict 输入，
    以及**正常成功响应绝不能被误判**。
  - `auth_error_message`：有 message 时带 code+message、只有 code 时带 code、
    空白 message + code=None → 兜底、非 dict → 兜底、`message` 优先于 `msg`。
  - `origin_from_url`：保留协议+主机+端口；缺协议或缺主机时**必须抛 ValueError**，
    不能返回一个能用的假源去打请求。
* **`app/automation.py`**（新建 `tests/test_automation_parsing.py`，35 条）
  - `parse_meal_rows`：把网站订单表格的「商品名 + 数量」解析成 `MealInfo`，
    **总餐次 / 经济·豪华 / 份数**全靠它，错了会直接写错排单表。覆盖：按餐别筛选、
    无标签行被忽略、缺键不崩、总餐次（六餐→6 / 单点→1 / 其它→None）、档次
    （经济优先于豪华）、份数（`xN` 不区分大小写、`共 x4 份` 也能解析、无 `xN`
    默认 1）、**标签后的文字属于下一片段会被丢弃**（`segments[:-1]` 的既有语义）、
    一行多标签各产一条且共享份数、空片段跳过、多行保序。
  - `extract_product_note_text`：按候选链取**第一个有数据**的选择器（空的和抛异常
    的都跳过）、首行是品名其余是备注、无备注/全失败返回空串、多行备注用空格连接
    且丢空行、无 `css` 的候选被跳过。
* **`app/processing.py`**（补进 `tests/test_processing.py`）
  - `split_text_and_number`：拆成（非数字部分, 数字部分）；`A10号楼 → ("A号楼","10")`
    （数字**全部**抽走）、`123 → ("","123")`、`None`/空白 → `("","")`。
  - `get_weekday_fill_value`：写的是**次日**的周几（周日跑 → 周一），七个键都在、
    只有恰好一个值为 1；遍历一周七天逐一验证。
* **`app/sss.py`**（补进 `tests/test_sss.py`）
  - `expected_delivery_date`：**16 点顺延次日**的边界（00/09/15 点 → 运行日；
    16/19/20/23 点 → 次日）；与 `compute_delivery_time` **永远一致**（24 小时逐一比对）；
    跨月、跨年边界。这条规则错了会**给平台报错日期**，且云端名单的「识别日期」必须
    与它一致，否则程序拒绝下单。
* **`app/update_health.py`**（补进 `tests/test_webview_app.py`）
  - `begin_update_health_check`：token 唯一且为 32 位十六进制、标记文件在指定目录内、
    文件名含 pid 与 token 前 8 位、**只算路径不建文件**；用 `monkeypatch` 固定
    `secrets.token_hex` 以确定性覆盖「清理陈旧标记」那一行；目录不存在也不抛错。
* **`app/address_aliases.py`**（补进 `tests/test_address_aliases.py`）
  - `aliases_path` / `pending_path`：文件名正确且**同目录**。

**用变异测试验收（沿用第 4 轮方法论）**

在 `/tmp` 副本上注入 **19 个真实 bug**，看新测试是否 FAIL。首轮得分 **84%（16/19）**，
3 个存活变异**全部是真缺口**，且都属于同一类毛病 —— **fixture 里两种写法都含关键字，
所以放宽判定也测不出来**：

| 存活变异 | 为什么没抓到 | 补的断言 |
|---|---|---|
| 六餐判定从 `"六餐"` 放宽成 `"六"` | 用例用「豪华六餐」，两种写法都命中 | 补「六号套餐」→ 必须 None |
| 经济判定从 `"经济"` 放宽成 `"经"` | 用例用「经济餐」，两种写法都命中 | 补「经典型套餐」→ 必须 None；顺带补「豪享套餐」防 `"豪"` |
| 备注候选从「取第一个」改成「取最后一个」 | 原用例前面候选**为空**，`break`/`continue` 结果一样 | 补「两个候选都有数据」→ 必须取第一个，且不再试后续 |

复测得分 **100%（19/19）**。

**无功能变更 —— 证据**
1. `git diff --quiet -- app/ frontend/` **通过**：产品代码与第 4 轮**逐字节相同**，
   本轮只新增/修改 `tests/` 下的文件。
2. 变异测试全程在 `/tmp/mut5` 副本进行，已核实副本清理、真实仓库未被污染。
3. 全套 **612 passed / 4 skipped**（本轮 +86 条）；ruff、compileall、--self-check、
   版本一致性、工作区卫生、前端 lint + test 全绿。

### 第 4 轮（2026-09-16）：补齐纯函数测试缺口 + 变异测试验收

**优化项（工程化规范 / 可维护性，零产品代码改动）**

用静态覆盖率代理（扫描 `tests/` 里是否**调用**过每个公开函数）排查后发现：
`tests/` 里**没有任何一处**直接调用过 `app/config.py` 的规整函数（只有 `AppConfig`
被用过），而这些纯函数是「配置正确性」的守门人 —— 规整错了就会写到**错误的云端
表格**。`app/sss_import.normalise_phone` 同样从未被直接调用过，它决定手机号能否
通过校验，进而决定某个客户**会不会被下单**。

新增 `tests/test_config_normalization.py`（71 条）锁住以下行为：

* `normalize_wps_tables`
  - 末尾会**丢掉没有 file_id 的条目**（未配置的子表不参与同步）；
  - 空白覆盖**不能**冲掉底表已有值；`drive_id` 同理；
  - 支持 `str` 与 `dict` 两种条目形态；
  - strip 子表名与取值；非字符串/空白键被跳过；未知类型被忽略；
  - `base` 在底表之上叠加新子表；
  - **传空 dict 作为 base 会被 `base or default` 判为假值 → 仍用出厂默认**
    （falsy 兜底是有意为之，写进测试避免误以为「base={} 就能清空同步目标」）。
* `normalize_wps_address_order`
  - **显式空列表是有效值**（= 该表按地址升序）—— 文档明确记录踩过「空即忽略」的坑；
  - 支持多行字符串（`splitlines`）/ `list` / `tuple`；非法类型保留底表；
  - 条目 strip、空条目丢弃；返回的 list **互相独立**。
* `normalize_wps_test_tables`
  - **默认必须为空**（避免回退到过期副本，否则测试模式会写错表）；
  - 要求子表名与 file_id **两侧都非空**。
* `normalize_wps_production_tables`
  - 必须以**独立的正式表备份**为底表 —— 文档记录的坑：拿「当前生效表」当底表，
    备份就失去意义。
* `default_wps_tables` / `default_wps_address_order` / `default_wps_production_tables`
  - 必须返回**深拷贝**（含嵌套 dict 与 list），不得共享模块级可变对象，
    否则一处修改会污染全局。
* `clamp_split_ratio`
  - 夹紧到 `[0.30, 0.55]`；`NaN` / 非数值 / 不可转换对象回退 `0.38`；
    数值字符串可用；遍历 -0.2~1.19 保证**永不越界**。
* `normalise_phone`
  - `bool` 分支（`True` 是 `int` 子类 —— 天真实现会得到 `"1"`/`"True"`）、`None`；
  - 精确整数 `float` 可接受（openpyxl 会把整数读成 float），**非整数 float 必须
    拒绝**（否则 `138.5` 会被截断成 `138`）；
  - 全角数字走 NFKC 转半角；去掉所有空白；
  - 并**明确记录它只做规范化、不做长度校验**（长度/纯数字校验在
    `validate_orders` 用 `_PHONE_RE` 负责），防止有人误以为「返回非空即合法」。
* `address_group` / `should_skip_address`
  - 决定「谁不需要闪时送」：`小西 → 小`、忽略大小写与空格（`B5`/`b5`/`B 5` 同组）；
  - **空地址不能被当成「小」而跳过**，否则会**静默漏单**。

另补 4 条锁住第 3 轮遗留问题：**失败「耗时」是否影响多发调用上界**。
曾担心「云端秒失败时线程池会取消同批兄弟任务，而 200ms 后才失败时兄弟已跑完，
两种时序下多发量是否都还有界」—— 实测（延迟 0 / 50 / 200ms × 多个失败位置）
多发一律 **≤ workers-1 张表 × 3 次**，异常文案也逐字相同。

**用变异测试验收（本轮新增的方法论）**

只写测试的轮次，「测试通过」可能只是因为断言太弱。因此在 `/tmp` 副本上往
`config.py` / `sss_import.py` **注入 15 个真实 bug**，看新测试是否会 FAIL：

| 注入的 bug | 是否被抓到 |
|---|---|
| `clamp_split_ratio` 不再夹紧、直接返回原值 | ✅ |
| `clamp_split_ratio` 的 NaN 兜底改成 0.5 | ✅ |
| `normalize_wps_tables` 不丢掉没有 file_id 的条目 | ✅ |
| `normalize_wps_tables` 空白覆盖冲掉底表值 | ✅ |
| `address_order` 把显式空列表当成「无值」忽略 | ❌ → 已补断言 |
| `address_order` 不再 strip 条目 | ✅ |
| `test_tables` 回退到正式表（过期副本坑） | ✅ |
| `production_tables` 底表错用「当前生效表」 | ❌ → 已补断言 |
| `default_wps_tables` 返回共享常量（不再深拷贝） | ✅ |
| `default_wps_address_order` 返回共享常量 | ✅ |
| `normalise_phone` 去掉 bool 守卫 | ✅ |
| `normalise_phone` 允许非整数 float | ✅ |
| `normalise_phone` 去掉 NFKC | ✅ |
| `should_skip_address` 去掉非空守卫 | ❌ 等价变异（见下） |
| `address_group` 去掉别名映射 | ✅ |

首轮得分 **80%（12/15）**，暴露 2 个**真缺口**并已补：

1. 「显式空列表是有效值」那条用例原先挑的是**医学院中餐**，而它的默认值本来就是
   `[]`，所以「空列表被忽略」的 bug 也能让断言通过。改挑默认值非空的**衣锦中餐**
   （默认 `["外卖柜","校门口"]`），并补上「显式 base 也要被空列表覆盖」与
   「多行空串同样算显式清空」。
2. `normalize_wps_production_tables` 的「底表必须独立」原先靠对比
   `DEFAULT_WPS_PRODUCTION_TABLES` 常量 —— 但 `DEFAULT_WPS_TABLES` 目前**恰好等于**
   它，所以「底表错写成 `default_wps_tables()`」抓不到。改为 **monkeypatch 把
   `DEFAULT_WPS_TABLES` 换成「测试副本_ID」，人为造出两者分叉**（这正是测试模式下
   的真实场景），再断言正式表备份不受影响。

复测得分 **93%（14/15）**。唯一存活的变异经核对是**等价变异**：`should_skip_address`
里的 `bool(key) and` 在当前常量下是冗余的（`_SKIP_ADDRESS_KEYS` 只有「大西」「小」，
空地址的 key 是 `""`，本来就不在集合里），去掉它行为完全相同 —— 已在测试里写明，
避免后人误判为缺口。**产品代码一行未改**（不允许改）。

### 第 3 轮（2026-09-16）：`build_plan` 云表读取按子表分批并发化

**优化项**：`app/wps_cloud.py::build_plan` 的 6 张子表**串行**读云端（每张 3 次往返：
`sheets_info` + `read_grid` + `read_grid(with_format=True)`，共 **18 次**，实测 **2.711s**，
**100% 网络等待**）。把循环体**原样抽成** `_build_sheet_plan(...)`，由 `build_plan`
按子表**分批并发**（`WPS_PLAN_WORKERS = 2`，`Executor.map` 保序）。实测
**2.711s → 1.357s，2.00x**，调用次数 18 vs 18 不变。`build_plan` 每次同步被预览与
上传各调用一次，因此每个同步会话省约 **2.7s**。

**并发度为什么只取 2**：金山接口 `429002 = 短时间频繁触发`（`wps_cloud.py:96,671`），
而 `_TRANSIENT_HINTS`（`~1695`）**不含限流提示**，`_run` **不会重试**它 —— 突发触发
会让该子表直接报错（列为 `unreadable`），属于可观测的行为偏差。取 2 只让瞬时速率
翻倍，且与 `bridge.WPS_COPY_CHECK_WORKERS` 保持同一口径。

**注意**：`build_plan` 是**只读**的（生成写入计划，不写任何东西）；真正的写路径
`apply_plan` **一行未动**。

**无功能变更 —— 证据链**：

1. **「纯搬移」已机械证明**：抽取用脚本完成（不是手抄），随后把基线 `01308b6` 的
   循环体与新的 helper 体各自 `textwrap.dedent` 归一化后**逐行比对** ——
   **295 行完全一致**，唯一差异是 5 处 `plans.append(plan)[+continue]` → `return plan`。
2. **删除行清点**：`git diff -w` 显示实质改动只有 **81 增 / 29 删**（652 个 diff 行里
   571 行是纯缩进变化）。29 处删除逐条可解释：旧签名与 docstring（已重新加入，
   签名不变）、2 行前导（`order_map` / `plans` 初始化）、4 行循环机制
   （`for` 头 + `conf` 三行）、5 个 `append` + 4 个 `continue`；
   **未归类删除行 = 0**。
3. **对外接口面**：AST 比对 `wps_cloud` 的 **50 个公开函数/方法**，名称与签名完全一致。
4. **基线差分对拍**：`01308b6` 整树检出到 `/tmp/yikou-base-01308b6`，同一 driver 在
   **300 组随机多子表场景**（0~6 张表、随机表头含缺列、随机不可读/读失败、随机
   `address_order`/`sort`/`target`）上对拍：返回 `SheetPlan` 的 `repr` **0/300 不一致**，
   异常类型与文案也逐字一致。
5. **多发调用有界且只在出错路径**：300 组里 11 组调用多重集不同，**11/11 都是基线
   抛异常的场景**；单场景最多多发 **3 次**（= workers-1 张表 × 3 次），符合上界。
6. **边界与确定性自查**：同场景并发跑 50 次产出唯一结果；多张子表共用同一
   `file_id` 仍生成 6 份计划且保序；1 有效 + N 无效 → 1 份；全部无效 → 0 份且零调用；
   异常类型与文案在并发下保持。
7. **测试**：新增 `tests/test_wps_cloud_build_plan_concurrency.py`（20 条）。
   ⚠️ 改动前 `tests/test_wps_cloud.py` 里 ~50 处 `build_plan` 调用**全是单表**，
   **多子表顺序完全没有覆盖** —— 本文件补上：多表顺序（含「完成顺序与提交顺序
   相反」的场景）、串行↔并发逐字段相等、调用次数与参数（含 `read_fmt` 的子区间
   参数 `(1,2,200,0,11)`）、缺 `file_id` 与幽灵表不发请求、空输入、单表走顺序分支、
   并发确实生效（`max_inflight ≥ 2` 且更快）、默认并发度 ≤ 2、并发度不会被配成 0、
   异常传播与多发上界、非 `WpsCloudError` 异常不被吞。
8. 全套 **451 passed / 4 skipped**；ruff / compileall / self-check / 版本一致性 /
   前端 lint+test 全绿；CI 8/8（含两个 Windows）。

### 第 2 轮（2026-09-15）：`wps_check_copies` 云表读取按表并发化

**优化项**：`app/bridge.py::Bridge.wps_check_copies` 原本对 6 张子表**串行**读云端
（每张表读副本 + 正式表，最多 12 次只读往返），改为**分批并发**（`ThreadPoolExecutor`，
`WPS_COPY_CHECK_WORKERS = 2`）。

**并发度为什么只取 2**：金山接口 `429002 = 短时间频繁触发`（`wps_cloud.py:96,671`），
而 `_TRANSIENT_HINTS`（`wps_cloud.py:1695`）**不含限流提示**，`_run` 不会重试它 ——
突发触发会让该表从「已核对」变成「读不了」，是可观测的行为偏差。取 2 只让瞬时速率
翻倍；这也是项目里最保守的既有取值（`sss.py` 验证码轮询同样用 2）。

**采用子代理做方案反证与边界验证**（本轮新增的工作方式）：
两个子代理分别做「并发安全性/技术调研/热点普查」与「对抗式等价性反证」。
反证代理用「把 01308b6 的方法源码 exec 进 BaselineBridge 子类」的差分夹具，
配真实 `KdocsCli` + 假 kdocs-cli 二进制，**发现并修复了 2 个真实缺陷**：

- **D1（已修）**：原先一次性 `pool.map` 提交全部任务，当 `read_grid` 抛出
  **非 `WpsCloudError`** 的意外异常（如 `wps_cloud.py:748` 的 `int(cell["originRow"])`
  遇到 `null`）时，已提交的任务收不回来，最多白耗 n-1=10 次云端调用，且报错延迟
  （1.5s → 4.0s）；串行版本出错即停。**改为按 workers 分批提交**：出错时后续批次
  不再发出，多发调用上界降到 `workers-1`（默认并发 2 即最多多发 **1** 次）。
  实测：第 1 次调用抛错时 workers=1→1 次、workers=2→1 次、workers=4→3 次。
- **D2（已修）**：`WPS_COPY_CHECK_WORKERS` 为 0/负数时 `ThreadPoolExecutor`
  抛 `ValueError`；改为 `max(1, min(...))` 兜底（原常量无 config/env 入口，不可达）。
- 同时**修正了过度声明**：常量注释与提交信息原先写「调用次数分毫不变」，
  现明确限定为「正常路径（含全部 5 种 status 与 `WpsCloudError`）不变；
  仅意外异常时最坏多发 workers-1 次」。

**无功能变更 —— 证据链**：

1. **接口面**：AST 比对 `Bridge` 的公开方法，37 个方法名/签名**完全一致**。
2. **基线差分对拍**：把基线 `01308b6` 整树检出到 `/tmp/yikou-base-01308b6`，
   同一 driver 在 **400 组随机场景**上跑「串行 vs 并发」：返回结果 **0 处不一致**
   （含 `json.dumps` 字节级与键序）；云端调用的**次数与参数（多重集）0 处不一致**；
   仅部分场景调用*顺序*不同（并发固有，对独立只读调用不可观测）。
   覆盖全部 5 种 status：drifted 607 / unreadable 192 / production_unreadable 106 /
   same_as_production 291 / aligned 30。
3. **反证代理的独立结论**：返回值/异常类型与文案/日志行/顺序在**所有**场景逐字节一致；
   `KdocsCli` 确认线程安全（3 个只读标量、无缓存、无全局写入；12 线程 × 15 次真实
   子进程并发读 180/180 正确、无临时文件与线程泄漏）；无死锁（pywebview 每个 js_api
   调用自带新线程，无嵌套池）；`workers ∈ {0,-1,-5} × n ∈ {0,1,2,6}` 全部覆盖。
4. **diff 审查**：`app/bridge.py` 仅数十行增删，删除部分全是结构搬移
   （`for` 头 + `results.append(item); continue` → `return item`），比较逻辑一行未动。
5. **测试**：新增 `tests/test_bridge_wps_copy_check.py`（16 条）钉死等价性：
   串行↔并发结果全等、调用次数与参数不变、`tables`/`drifted` 顺序不变、5 种状态
   字段契约、`effective_tables` 异常仍在 try 外传播、**意外异常不吞且不发后续批次**、
   并发度不会被配成 0、默认并发度上界 ≤ 2、并发确实生效（`max_inflight ≥ 2` 且更快）。
   连跑 5 轮稳定。
6. 全套 **431 passed / 4 skipped**；ruff / compileall / self-check / 版本一致性 /
   前端 lint+test 全绿。

**实测提速**（模拟云端读 RTT=150ms，6 张子表 12 次调用）：串行 1.802s →
并发(workers=2) 约 0.95s；调用次数 12 vs 12 不变。

**该改动的先例**：`app/sss.py` 早已用 `ThreadPoolExecutor` 并发下单，
并有 `test_submit_tasks_concurrent_faster_than_serial` 同类测试——本次只是把同一
模式用到云表只读核对上，风格与项目一致。

**下一轮最高价值目标（子代理普查结论）**：`app/wps_cloud.py:971` `build_plan`
的按子表读循环，最多 3 次往返 × 6 张表 = 18 次串行调用，且预览与上传各跑一次
≈ 36 次（约占每日额度 150~200 的 20%）。已验证循环体无副作用（`ledger`/`log`
两个参数在 `build_plan` 内**从未被使用**，写账本都在 `apply_plan`），
风险 LOW-MED。**绝对不要并发化**：`apply_plan` 写循环（有序依赖 + 共享
`SyncLedger` 无锁）、任何 Playwright `page.*` 循环（同步 API 线程亲和）、
`learn_row_format` 的逐列回读兜底（在写路径中间）。

### 第 1 轮（2026-09-15）：基线体检 + 撤销一处「误读需求」的实现

- **项目基线体检**：415 passed / 4 skipped，`ruff check` 干净，前端 lint 干净、
  2 条桥接游标测试通过，版本一致性通过，确认代码库处于健康状态。
- **一次重要的误读与纠正（值得记住的教训）**：
  1. 目标里写「当上下文容量逼近 100MB 阈值时自动触发压缩」，我**误读**成
     「程序的诊断产物（`logs/` 失败快照 + `update.log`）需要一个 100 MiB 预算」，
     于是实现了 `app/artifact_store.py`：五档策略（轮转超大日志 → 淘汰超量归档 →
     归档证据组(无损) → 淘汰最旧归档 → 清理最旧证据(兜底)），并在启动时挂后台
     线程自动执行。当时还修了自身三个缺陷：预算误把**不可管**的 `webview/` 缓存
     算进去（会误判超标、白删证据却永远达不成目标）、有损档位门槛过低导致白删
     证据（140MiB→44.8MiB、明明还有余量）、归档保留顺序搞反（保留了最旧的 24 组、
     反而清掉较新的 11 组，与「排查问题看最近的失败」相反）。
  2. 用户澄清：**「100M 指当前这个对话内容的上下文，快要逼近 100 万 token 的时候
     自动压缩当前对话的上下文，而不是项目里面的日志。」**
  3. 于是**全部撤销**该实现：`app/main.py`、`app/webview_app.py`、`app/__init__.py`、
     `README.md` 还原到 `01308b6`，删除 `app/artifact_store.py` 与
     `tests/test_artifact_store.py`。验证 `git diff 01308b6 -- app/` **为空**、
     `frontend/` 零改动、测试回到原始的 415 passed / 4 skipped。
     代码可从 `git show 3a24ef1` 找回。
  4. 顺带查明：CI 上那次 `failure` 正来自该被撤销的代码 —— 它在 Windows 上用
     `write_text` 写归档，`\n` 被转成 `\r\n`，导致字节比对失败；撤销后 CI 在
     所有平台通过。这也说明「未经真机验证的代码不要留在主分支」。
- **交付 0 处功能变更**：`app/` 与 `frontend/` 均未改动。
- **新增本文件**，作为跨轮次持久记忆。注意：用文件承载跨轮状态**不等于**要把它
  写得很短 —— 它是项目的完整记录，该详实就详实（见开头说明）。

## 下一步候选（按「非侵入式」价值排序）

0. **【待用户决策】`save_wps_config` 的 `tables` / `address_order` 部分回传会重置其它子表**：
   这两个字段走 `normalize_*(payload.get(...))`，以**出厂默认**为底而不是**当前配置**为底，
   所以只回传部分子表时，没提到的子表会被重置成出厂 `file_id`（第 17 轮实测复现）。
   同方法里其它字段都是「缺键保留原值」的白名单语义，两者不一致。**当前前端回传完整
   列表，实际不会触发**；修法（`base=cfg.wps_tables`）属**行为变更**，需用户拍板。
   实际行为已由 `tests/test_bridge_settings_api.py` 钉住。

1. **前端测试缺口（最大的剩余空白）**：`frontend/src/lib/` 只有 2 条测试（事件游标与
   去重），`frontend/src/lib/bridge.ts`（646 行）其余逻辑、`frontend/src/hooks/useApp.tsx`
   （344 行）、`frontend/src/hooks/appContext.ts`、以及各组件（`TaskPanel.tsx` 724 行、
   `CloudForm.tsx` 504 行…）全部零覆盖。`pnpm test` 现在用
   `node --test src/lib/*.test.ts`（无 DOM、无 React 渲染）。**难点与可选路径**：
   - 组件内的纯函数（如 `CloudForm.splitAddressLines`、`LogConsole.isOrderSummary` /
     `formatLogMsg`、`TaskPanel.modeError`、`fields.startOfMonth` / `sameDay` /
     `formatISO`）目前**未导出**，要测必须先 `export` —— 这属于**加宽模块表面积**，
     虽不改运行时行为，但仍算**产品代码改动**，需要用户确认是否接受；或者
   - 引入 `jsdom` + React 测试库做组件渲染测试 —— 会给 `package.json` 增加
     devDependencies（并改动 lockfile）。同样建议先确认。
   - 唯一**零改动**可测的是已导出的 `appContext.statusLabel`（很小）。
2. 其余可继续用「静态缺口 + 变异测试」推进的方向：
   - `app/bridge.py` 里尚未直接测过的 js_api 方法（第 2 轮已验证这类方法可以用假
     CLI / 假 config 测，如 `wps_status`、`wps_preview`、`save_wps_config` 等）；
   - `app/config.py` 的 `AppConfig.load` / `save` 往返与容错（只在别的测试里被间接用过）；
   - `app/credentials.py`（9/9 未覆盖，但依赖系统密钥链，需 monkeypatch keyring）。
3. 补 `app/` 缺失的模块级 docstring / 类型标注（纯注释，零风险）。
4. 全仓 `ruff format`（55 个文件未格式化）：需**单独一轮**，避免和其它改动混在一起
   制造大 diff（CI 只跑 `ruff check`，已确认）。

**禁区（不要并发化，子代理普查结论 + 已复核）**：
* `apply_plan` 的写循环 —— 有序依赖（`insert_rows` → `write_cells` →
  `write_format_ops` → `sort_range` → `delete_columns`）且共享**无锁**的 `SyncLedger`；
* 任何 Playwright `page.*` 循环 —— 同步 API 线程亲和，
  `automation.py` 自己就写了这条规则；
* `learn_row_format` 的逐列回读兜底 —— 在写路径中间；若要动，正确方向是**让格式
  缓存读取更可靠**，不是并发化。

**方法论（每轮照做）**：
* 任何**并发化**改动 → 基线整树检出 + 随机场景**差分对拍** + `textwrap.dedent`
  归一化证明「纯搬移」+ 清点 `git diff -w` 的删除行。
* 任何**只写测试**的改动 → **变异测试**验收（在 `/tmp` 副本注入真实 bug），
  并甄别**等价变异**。
* 每轮都做：AST 比对对外接口面、`app/` 与 `frontend/` 改动范围审查。

## 已知本地噪声（非缺陷，勿"修"）

- `scripts/check_workspace_hygiene.py --working-tree` 在本机会报
  `.zcode/sss_handoff/flash_*_20260909.*`：那是**用户自己的业务数据**，已被
  `.gitignore` 忽略、不会进 CI，不要删除。
- 仓库根有 `yikou-light-food-linux-x64.tar.gz`（145 MB 本地构建产物，已忽略）。
- Windows CI 作业在本机网络下曾长时间排队（>22 分钟）；Linux/macOS 六个作业均通过。
