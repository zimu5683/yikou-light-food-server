# 迭代进展与上下文交接

> 本文件是**跨轮次的持久记忆**：每一轮自主迭代开始时先读这里，不要重新全仓扫描。
> 每轮结束前把「已完成 / 已验证 / 下一步」更新到本文件，控制在 100 行以内。

## 硬约束（每轮必须遵守）

1. **功能不变原则**：不改动核心业务逻辑、对外接口与既定功能行为，只做**非侵入式**
   优化（可维护性 / 执行效率 / 工程化规范）。
   *自检*：`git diff <上轮基线>..HEAD -- app/ | grep '^-' | grep -v '^---'`
   应当只剩版本号一行；`sss.py` / `wps_cloud.py` / `bridge.py` / `automation.py` /
   `updater.py` / `frontend/` 应**零改动**。
2. **每轮推送 GitHub**：`git push origin main` 完成版本备份与归档。
   推 `main` 只触发 Tests；**打 `vX.Y.Z` 标签才会触发公开 Release 构建**。
3. **上下文自适应压缩**：实现见下文 v3.5.0；每轮先读本文件，不要重扫全仓。

## 基线（每轮验证命令）

```bash
.venv/bin/python -m pytest tests -q          # 期望：453 passed, 4 skipped
.venv/bin/python -m ruff check app tests scripts
.venv/bin/python scripts/check_version_consistency.py
.venv/bin/python -m app.main --self-check
python scripts/check_workspace_hygiene.py --working-tree   # 见「已知本地噪声」
```

## 项目速览

- 桌面程序：pywebview（React + TS + Tailwind 前端）+ Playwright + openpyxl。
- 三个任务模式：订单处理、云文档同步（WPS 云端排单表增量写入）、闪时送下单。
- 默认**纯接口模式**（不发浏览器），可在界面切回浏览器模式。
- 关键模块：`app/bridge.py`（js_api 桥 + 事件通道）、`app/sss.py`（闪时送）、
  `app/wps_cloud.py`（WPS 云表）、`app/updater.py`（Ed25519 签名自动更新）、
  `app/artifact_store.py`（**本轮新增**：诊断产物预算与自适应压缩）。
- 版本号唯一来源：`app/__init__.py`；发布靠推 `vX.Y.Z` 标签触发三个平台工作流。

## 已完成

### v3.5.0 — 上下文自适应压缩（本轮）

- 新增 `app/artifact_store.py`：受管产物（`logs/` 证据快照 + `update.log`）总占用
  的「预算 + 逼近触发 + 自适应压缩」。
  - 阈值 **100 MiB**，`trigger_ratio=0.8`（80 MiB 触发），`target_ratio=0.6`。
  - 五档动作，信息损失从小到大：轮转日志(无损) → 淘汰超量归档 → 归档证据组(无损)
    → 淘汰最旧归档 → 清理最旧证据(兜底)。
  - **有损档位只在真的突破 `budget_bytes` 时启动**，避免"能压却先删"。
  - 第 3 档由新到旧、第 5 档由旧到新 ⇒ 越新的现场越先被保住。
  - `fresh_keep=5` 是最新 5 组现场不可触碰的硬下限。
  - 预算**只统计受管产物**（`logs/` + `update.log`）：`webview/` 缓存、`config.json`
    等既管不了也不该算，否则会误判超标、白删证据却永远达不成目标。
- 接入点：`app/webview_app.run()` 启动时后台线程；`app/main.py --compact-artifacts
  [--dry-run]`；`--self-check` 增加 `app.artifact_store` 导入校验。
- 测试：`tests/test_artifact_store.py`（38 条），覆盖无损还原、幂等、预演、
  失败隔离、不越界、受管范围、保留顺序、收敛终止。
- 实测（40 组共 140.2 MiB 现场 + 6 MiB `update.log` + 30 MiB 不可管 webview 缓存）：
  **140.2 MiB → 81.4 MiB，40 组现场一条不丢**；二次运行幂等、摘要如实说明无事可做。

## 下一步候选（按「非侵入式」价值排序）

1. 前端 `frontend/src/hooks/useApp.tsx` 的事件游标/去重逻辑补单测
   （纯新增测试，零风险；`frontend/src/lib/bridge.events.failure.test.ts` 已有先例）。
2. `app/bridge.py::wps_check_copies` 与云表逐格回读目前串行，可安全并发
   （纯执行效率优化，接口不变 —— 注意需保持调用次数不变，云端有每日额度）。
3. `logs/` 归档可检索性：归档目前只能靠时间戳文件名，可补索引（**只读新增**）。
4. 若要做行为变更类优化，先确认「功能不变」边界，或明确询问用户。

## 已知本地噪声（非缺陷，勿"修"）

- `scripts/check_workspace_hygiene.py --working-tree` 在本机会报
  `.zcode/sss_handoff/flash_*_20260909.*`：那是**用户自己的业务数据**，已被
  `.gitignore` 忽略、不会进 CI，不要删除。
- 仓库根有 `yikou-light-food-linux-x64.tar.gz`（145 MB 本地构建产物，已忽略）。
- `ruff format --check` 全仓 55 个文件未格式化；CI **只跑 `ruff check`**，
  不要为了"格式化"制造大 diff。
