# M7 功能对照验收清单（v3.0.1 发布前）

> 对照基准：旧 Tkinter 版功能面穷举清单（探索报告）。
> ✓ = 已自动化验证或用户实测通过；○ = 已实现、依赖用户后续真实账号流程抽查。

| # | 功能点 | 状态 | 依据 |
|---|---|---|---|
| 1 | 模式切换（订单处理/闪时送下单） | ✓ | 分段 tab，切换渲染两套表单（截图矩阵） |
| 2 | 订单表单字段全集 + helper 文案 | ✓ | 截图与代码比对 |
| 3 | 闪时送表单字段全集（含商品名称/常用地址） | ✓ | 同上 |
| 4 | 字段三态 + 校验错误文案（必填/Excel 存在性/后缀/日期格式/单数 1–9999） | ✓ | 桥接 e2e：空表单返回全部字段错误 |
| 5 | 启动前保存 config、remember 写 keyring | ✓ | 桥接 e2e + 真实运行回填（截图含真实数据） |
| 6 | Excel 选择对话框（.xlsx/.xlsm 过滤） | ○ | js_api create_file_dialog 已接；用户日常使用验证 |
| 7 | 新建模板（排单/闪时送，openpyxl） | ✓ | 单元验证文件生成 + 字段回填 |
| 8 | 日期选择器（仅今天/过去、可清空） | ✓ | 组件实现 + mock 截图；真实点击待用户抽查 |
| 9 | 待处理订单数 Stepper（1–9999、可清空重输） | ✓ | 草稿态实现；用户实测 Backspace 修复后可用 |
| 10 | 开始/停止按钮状态联动（running 期间禁用/启用） | ✓ | 状态机 parity |
| 11 | 停止确认对话框（停止/继续/取消） | ✓ | AlertDialog 实现 |
| 12 | 单单失败决策（重试/跳过/停止，阻塞 worker） | ✓ | decision 事件 + resolve_decision 异步等价（e2e） |
| 13 | Excel 占用重试决策 | ✓ | 同上 |
| 14 | 运行日志实时显示（不丢行） | ✓✓ | 用户两轮真实任务：8/8 订单全部显示；25 条突发压测 0 丢失 |
| 15 | 日志过滤/命中高亮/复制/清空/自动滚动 | ✓ | 组件实现（复制输出含多行排版） |
| 16 | 状态徽章 6 态 + stopped 修正 | ✓ | 截图出现 就绪/处理中/检查更新/处理完成 等态 |
| 17 | 检查浏览器 | ✓ | 按钮接入 ensure_browser |
| 18 | 清除密码（双服务名） | ✓ | 确认框 + keyring 删除 |
| 19 | 更新检查（启动静默 + 手动） | ✓✓ | 用户截图出现"正在检查更新…/已是最新版本" |
| 20 | 更新下载进度模态（不可关）+ 完成自毁重启 | ✓ | update:progress/stage/installed 事件 + 模态实现 |
| 21 | 关闭保护（任务运行时三选） | ✓ | request_close + close_confirm 决策 |
| 22 | 分栏比例持久化 + <980px 堆叠 | ✓ | 拖拽分栏 + set_split_ratio；堆叠截图 |
| 23 | 双主题（浅/深）持久化 | ✓ | localStorage + 截图矩阵 |
| 24 | 闪时送图形验证码人工介入提示 | ✓ | 日志提示保留（流程本身不变） |
| 25 | 订单多行排版 / 日志可选中 / 无"接口读取"行 | ✓✓ | 本轮新增需求，用户实测确认 |
| 26 | 历史订单写入"日期 周几"归档表 | ✓ | 业务逻辑未动；已向用户说明位置 |

**遗留行为差异（有意为之）**：
- 浏览器缺失不再询问 Edge/Chrome，直接打开 Edge 下载页 + Toast；
- 更新失败不再弹窗询问，改为 Toast 提示（可从"更多→检查更新"重试）；
- 订单间"-------"文本分隔线移除（新界面有虚线裁切线）；
- 浏览器窗口最小化尝试已按用户要求**撤回**（Wayland 下 CDP/外部最小化均不可行，原 sss 实现从未生效）。

**自动化证据**：`frontend_report` 回传通道 4/4；桥接协议 e2e ALL PASS；pytest 91 passed；ruff/compileall 全绿；截图 `design/mocks/preview/`。
