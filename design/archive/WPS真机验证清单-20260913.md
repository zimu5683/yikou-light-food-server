# WPS 真机验证清单 —— 2026-09-13 早上（额度恢复后）

> 昨晚（09-12）本地调试已完成：测试 335 全过、夹具离线仿真与目标表逐格一致、
> 新版本已构建并部署到 `/home/zimu/下载/yikou-light-food-linux-x64(1)/yikou-light-food`。
> 今天唯一要确认的是 **insert-rows-cols 在真机上的行为**（此前从未真机调用过）。

## 0. 前置确认

- [ ] 额度已恢复：随便跑一次只读调用（`kdocs-cli sheet get-sheets-info`），
  若仍 `429001` 就改点再试。
- [ ] 试验田（东湖中餐 `H8vzKoTJVrMP7mA9QG591xqS9W8Bg57iG`）当前状态 = 基线副本
  （96 人、无 9.12 数据）。若内容有变，先 `python scripts/reset_wps_field.py --apply --sheets 东湖中餐`。

## 1. 插入行接口的最小验证（约 6 次调用，最优先）

用 `vendor/kdocs-cli/kdocs-cli sheet insert-rows-cols` 对试验田：
1. 读 `A11:N13` 附近原始内容留底；
2. 在第 12 行前插 1 行（`type=row, row_from=11, row_to=11`）；
3. 读回 `A11:N14`，确认：原 12~13 行下移、底色随行走、K~M 黄带位置不变；
4. 在插出的空行写一个临时值再读回；
5. `delete-range-data`（`type=row, row_from=11, row_to=11`）删掉，读回核对复原。

**若第 3 步发现下方公式/底色不随行下移** → 插入方案不成立：
把 `app/wps_cloud.py` 里 `InsertBlock.append_only` 全按 True 处理
（即所有新客户退回表尾追加），并如实告知用户"接口做不到按地址组插入"。

## 2. 记号列与记号值确认（只读，2 次调用）

- 读试验田表头行右侧（P/Q/R 列），确认备注右侧的 1~7 数字格在哪一列；
- `find_marker_column()` 应定位到它（预计 Q 列，当前值 5）。
- 若实际不在 Q 列也没数字 → 看一眼 6 张正式表的表头截图/读值再定，不要硬写。

## 3. 正式跑一遍（只写试验田，仍处测试模式）

1. 启动已部署的程序，选好排单表（用户的本地排单 xlsx）；
2. 先点「云同步预览」：确认 25 个新客户全部显示"插入到第 N 行（「XX」组末尾）"、
   行号与夹具目标表一致（12 / 34-40 / 44 / 52 / 60 / 63 / 75-76 / 87-89 / 114-120 / 123）；
3. 点「开始云同步」写东湖中餐（另外两张中餐表先不动）；
4. 回读校验通过后，浏览器打开云端表人工核对：
   - 新行位置、值、K~M 金黄、豪华行（陈章依/灵/刘姵怡）整段金黄；
   - 下方老客户的公式（剩余餐列）是否仍然正确（没有被插行打乱）；
   - Q 列记号 = 6（周五）；
5. 与《测试东湖中餐.xlsx》的目标表逐人比对（可用
   `tools/simulate_wps_fixture.py` 的比对思路写个只读核对脚本）。

## 4. 通过后

- 东湖中餐 ✅ → 同样方式跑衣锦中餐、医学院中餐（用户指定的顺序）；
- 全部 ✅ → 报告用户，等用户点头后 `bridge.restore_wps_production_tables()` 切回正式表。

## 5. 已知可接受差异（比对时不要当 bug 报）

1. 新行的 已出餐/剩余餐 列：云端是公式，程序不写（导出对比时才显示差值）；
2. 豪华行备注列（N）：程序按用户要求整行金黄，昨天的目标表导出里该列没涂；
3. 灵/刘姵怡在 D2 组尾的相邻顺序（数据相同，手工录入顺序差异）。

## 6. 关键文件

| 文件 | 作用 |
|---|---|
| `app/wps_cloud.py` | 插入块/记号/格式合并的新实现 |
| `tools/simulate_wps_fixture.py` | 夹具离线仿真（不联网） |
| `design/WPS-CLOUD-SYNC-PLAN.md` | 昨晚改动全记录（文末 2026-09-12 节） |
| `scripts/reset_wps_field.py` | 试验田重置（出问题时滚回基线） |
